---
phase: 10-analytics-reporting
plan: 04
subsystem: ui-database
requirements: [ANLYT-03, ANLYT-02, ANLYT-04]
---

# Plan 10-04: Database Rollup Fix & Dashboard UI Upgrade

**Goal:** Ensure the nightly rollup correctly calculates dwell time and update the Analytics Dashboard to show full interaction metrics (Avg Dwell Time, Engagement Rate, Video Plays).

## Tasks

1. **Task 1: Fix Nightly Rollup SQL (auto)**
   - Update `public.rollup_daily_metrics` function to sum `dwell_time_ms` from `extra_data` (matching `rollup_today_metrics`).

2. **Task 2: Update Analytics Dashboard KPI Cards (auto)**
   - Add cards for **Avg Dwell Time**, **Engagement Rate**, and **Video Completion Rate**.
   - Ensure the `use-analytics` hook correctly exposes these derived metrics.

3. **Task 3: Update Analytics Charts (auto)**
   - Ensure the daily trend chart can toggle between different metrics (Impressions, Clicks, Engagements).

## Verification
- Run a manual `SELECT rollup_daily_metrics('2026-03-03')` (if possible in SQL editor) or verify code structure.
- Verify Analytics Dashboard shows non-zero values for Engagements and Dwell Time (simulated data or local testing).
