---
phase: 10-analytics-reporting
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - apps/web/src/features/analytics/components/date-range-select.tsx
  - apps/web/src/features/analytics/components/kpi-cards.tsx
  - apps/web/src/features/analytics/components/metrics-chart.tsx
  - apps/web/src/features/analytics/components/metrics-table.tsx
  - apps/web/src/features/analytics/components/csv-export-button.tsx
  - apps/web/src/features/analytics/components/analytics-filters.tsx
  - apps/web/src/features/analytics/pages/analytics-page.tsx
  - apps/web/src/router.tsx
autonomous: true
requirements:
  - ANLYT-01
  - ANLYT-02
  - ANLYT-03
  - ANLYT-04
  - ANLYT-05
  - ANLYT-06

must_haves:
  truths:
    - "User sees KPI summary cards for impressions, clicks, CTR, engagements, and avg dwell time on the analytics page"
    - "User sees a time-series area chart showing metrics over the selected date range"
    - "User can change the date range using preset options (7d, 30d, 90d, this-month, last-month)"
    - "User can filter analytics by specific creative or campaign"
    - "User can download CSV export of the currently displayed analytics data including dwell time"
    - "User sees a tabular breakdown of metrics by creative and by campaign including avg dwell time column"
    - "Refreshing the page loads latest data (staleTime: 0 triggers fresh fetch)"
    - "Navigating to /analytics loads the real analytics dashboard (not placeholder)"
  artifacts:
    - path: "apps/web/src/features/analytics/components/date-range-select.tsx"
      provides: "Preset date range dropdown using shadcn Select"
      min_lines: 20
    - path: "apps/web/src/features/analytics/components/kpi-cards.tsx"
      provides: "5 summary KPI cards (impressions, clicks, CTR, engagements, avg dwell time)"
      min_lines: 35
    - path: "apps/web/src/features/analytics/components/metrics-chart.tsx"
      provides: "Time-series Recharts AreaChart with metric selector tabs including dwell-time"
      min_lines: 40
    - path: "apps/web/src/features/analytics/components/metrics-table.tsx"
      provides: "Tabular breakdown by creative or campaign with Avg Dwell column"
      min_lines: 40
    - path: "apps/web/src/features/analytics/components/csv-export-button.tsx"
      provides: "Download button wired to exportToCsv utility with dwell time in CSV"
      min_lines: 15
    - path: "apps/web/src/features/analytics/components/analytics-filters.tsx"
      provides: "Creative and campaign filter dropdowns"
      min_lines: 30
    - path: "apps/web/src/features/analytics/pages/analytics-page.tsx"
      provides: "Main analytics dashboard page composing all components, passing totalDwellTimeMs to KPI cards"
      min_lines: 60
    - path: "apps/web/src/router.tsx"
      provides: "Lazy-loaded /analytics route pointing to analytics-page"
      contains: "features/analytics/pages/analytics-page"
  key_links:
    - from: "apps/web/src/features/analytics/pages/analytics-page.tsx"
      to: "apps/web/src/features/analytics/hooks/use-analytics.ts"
      via: "useAnalytics hook call with date range and filters"
      pattern: "useAnalytics\\("
    - from: "apps/web/src/features/analytics/components/kpi-cards.tsx"
      to: "analytics-page.tsx"
      via: "avgDwellTimeMs prop from summary.totalDwellTimeMs"
      pattern: "avgDwellTimeMs"
    - from: "apps/web/src/features/analytics/components/metrics-chart.tsx"
      to: "recharts"
      via: "Recharts AreaChart import"
      pattern: "import.*from.*recharts"
    - from: "apps/web/src/features/analytics/components/csv-export-button.tsx"
      to: "apps/web/src/features/analytics/lib/csv-export.ts"
      via: "exportToCsv function call"
      pattern: "exportToCsv\\("
    - from: "apps/web/src/router.tsx"
      to: "apps/web/src/features/analytics/pages/analytics-page.tsx"
      via: "lazy import in /analytics route"
      pattern: "features/analytics/pages/analytics-page"
---

<objective>
Build the analytics dashboard UI with KPI cards, time-series charts, metrics table, date range selector, creative/campaign filters, CSV export, and wire the /analytics route.

Purpose: Deliver the user-facing analytics experience that displays impressions, clicks, CTR, dwell time, and engagement metrics per creative and campaign with configurable date ranges and export capability.

Output: Complete analytics dashboard page with all components, wired to the data layer from Plan 01, accessible at /analytics.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-analytics-reporting/10-RESEARCH.md
@.planning/phases/10-analytics-reporting/10-01-SUMMARY.md
@apps/web/src/features/billing/pages/billing-page.tsx
@apps/web/src/router.tsx
@apps/web/components.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analytics UI components (date-range, KPI cards, chart, table, filters, CSV button)</name>
  <files>
    apps/web/src/features/analytics/components/date-range-select.tsx
    apps/web/src/features/analytics/components/kpi-cards.tsx
    apps/web/src/features/analytics/components/metrics-chart.tsx
    apps/web/src/features/analytics/components/metrics-table.tsx
    apps/web/src/features/analytics/components/csv-export-button.tsx
    apps/web/src/features/analytics/components/analytics-filters.tsx
  </files>
  <action>
1. Create `date-range-select.tsx`:
   - Import `Select, SelectContent, SelectItem, SelectTrigger, SelectValue` from `@/components/ui/select`.
   - Import `DATE_PRESETS, DateRangePreset` from `../lib/analytics-types`.
   - Props: `{ value: DateRangePreset; onChange: (value: DateRangePreset) => void }`.
   - Render a shadcn Select with Calendar icon (lucide-react), default value '30d'. Each preset rendered as SelectItem.

2. Create `kpi-cards.tsx`:
   - Import Card, CardContent, CardHeader, CardTitle from `@/components/ui/card`.
   - Import icons: Eye, MousePointerClick, TrendingUp, Activity, Clock from lucide-react.
   - Props: `{ impressions: number; clicks: number; ctr: number; engagements: number; totalDwellTimeMs: number; loading?: boolean }`.
   - Render **5 cards** in a responsive grid (`grid-cols-1 sm:grid-cols-2 lg:grid-cols-5 gap-4`).
   - Card 1: Eye icon, "Impressions", formatted with `Intl.NumberFormat('en-US')`.
   - Card 2: MousePointerClick icon, "Clicks", formatted with `Intl.NumberFormat('en-US')`.
   - Card 3: TrendingUp icon, "CTR", `ctr.toFixed(2) + '%'`.
   - Card 4: Activity icon, "Engagements", formatted with `Intl.NumberFormat('en-US')`.
   - Card 5 (NEW — ANLYT-03): Clock icon, "Avg Dwell Time". Compute: `const avgDwellSec = engagements > 0 ? (totalDwellTimeMs / engagements) / 1000 : 0`. Display as `avgDwellSec > 0 ? avgDwellSec.toFixed(1) + 's' : '—'`. This gracefully shows "—" when total_dwell_time_ms is 0 (expected for historical rollups that did not extract dwell time from extra_data).
   - When `loading`, show Skeleton placeholders instead of values.

3. Create `metrics-chart.tsx`:
   - Import from `recharts`: AreaChart, Area, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer.
   - Import Tabs, TabsList, TabsTrigger from `@/components/ui/tabs`.
   - Import Card, CardContent, CardHeader, CardTitle from `@/components/ui/card`.
   - Import ChartDataPoint from `../lib/analytics-types`.
   - Props: `{ data: ChartDataPoint[]; loading?: boolean }`.
   - Internal state: `selectedMetric` — one of 'impressions', 'clicks', 'ctr', 'engagements', 'dwell-time'. Default: 'impressions'.
   - Render a Card with TabsList for metric selection in the header (**5 tabs**: Impressions, Clicks, CTR, Engagements, Dwell Time), and an AreaChart inside CardContent.
   - For 'dwell-time' tab: map data to use `avgDwellTimeMs / 1000` as the chart value (display in seconds). YAxis label: "seconds". Tooltip format: `value.toFixed(1) + 's'`.
   - AreaChart: `<ResponsiveContainer width="100%" height={350}>`. XAxis formats dates as 'MMM d' using `new Date(d + 'T00:00:00').toLocaleDateString('en-US', { month: 'short', day: 'numeric' })`. YAxis with auto domain. Tooltip shows full date and formatted value. Area with `type="monotone"`, stroke `hsl(var(--primary))`, fill with 0.1 opacity.
   - When loading or no data, show a centered message or Skeleton.
   - NOTE: Use Recharts v3 API directly. Do NOT use deprecated v2 patterns like `Customized` wrapper or `activeIndex` state props.

4. Create `metrics-table.tsx`:
   - Import Table, TableBody, TableCell, TableHead, TableHeader, TableRow from `@/components/ui/table`.
   - Import Tabs, TabsList, TabsTrigger from `@/components/ui/tabs`.
   - Import Card, CardContent, CardHeader, CardTitle from `@/components/ui/card`.
   - Props: `{ data: DailyMetricRow[]; creatives: { id: string; name: string }[]; campaigns: { id: string; name: string }[]; loading?: boolean }`.
   - Internal state: `view` — 'by-creative' or 'by-campaign'. Default: 'by-creative'.
   - For 'by-creative' view: group rows by creative_id, sum metrics (including total_dwell_time_ms), look up creative name from creatives prop. Show columns: Creative Name, Impressions, Clicks, CTR, Engagements, **Avg Dwell (s)**.
   - For 'by-campaign' view: group rows by campaign_id, sum metrics (including total_dwell_time_ms), look up campaign name from campaigns prop. Rows with null campaign_id labeled "Unassigned". Show columns: Campaign Name, Impressions, Clicks, CTR, Engagements, **Avg Dwell (s)**.
   - **Avg Dwell (s) column**: Compute per-group as `totalDwellTimeMs / max(engagements, 1) / 1000`. Display as `value > 0 ? value.toFixed(1) + 's' : '—'`. Shows "—" gracefully when dwell time is 0 (historical rollups without dwell extraction).
   - Use Intl.NumberFormat for numbers, toFixed(2) + '%' for CTR.
   - When loading, show Skeleton rows.

5. Create `csv-export-button.tsx`:
   - Import Button from `@/components/ui/button`, Download icon from lucide-react.
   - Import exportToCsv from `../lib/csv-export`.
   - Import DailyMetricRow from `../api/analytics-api`.
   - Props: `{ data: DailyMetricRow[]; dateRange: string }`.
   - On click: transform data rows into CSV format with headers ['Date', 'Creative ID', 'Campaign ID', 'Impressions', 'Viewable Impressions', 'Clicks', 'Engagements', 'Video Plays', 'Video Completes', **'Total Dwell Time (ms)'**]. Include `total_dwell_time_ms` as the last column value from each row. Filename: `analytics-${dateRange}-${today}.csv`.
   - Render a Button variant="outline" with Download icon and "Export CSV" text.

6. Create `analytics-filters.tsx`:
   - Import Select components from `@/components/ui/select`.
   - Props: `{ creatives: { id: string; name: string }[]; campaigns: { id: string; name: string }[]; selectedCreativeId?: string; selectedCampaignId?: string; onCreativeChange: (id: string | undefined) => void; onCampaignChange: (id: string | undefined) => void }`.
   - Render two Select dropdowns side by side: "All Creatives" / "All Campaigns" as default, with options from props. Selecting the "All" option clears the filter (passes undefined).
  </action>
  <verify>
    - All 6 component files exist under `apps/web/src/features/analytics/components/`
    - `cd apps/web && npx tsc --noEmit` passes (no type errors)
    - Each component has a default or named export
    - kpi-cards.tsx renders 5 cards (grep for "Avg Dwell Time" or "Clock" icon)
    - metrics-chart.tsx has 5 tabs including 'dwell-time' (grep for "dwell-time" or "Dwell Time")
    - metrics-table.tsx has "Avg Dwell (s)" column header (grep for "Avg Dwell")
    - csv-export-button.tsx includes 'Total Dwell Time (ms)' in CSV headers (grep for "Total Dwell Time")
  </verify>
  <done>
    Six analytics UI components ready: date range selector, KPI summary cards (5 cards including avg dwell time), time-series area chart (5 metric tabs including dwell time), metrics breakdown table (with Avg Dwell column), CSV export button (with dwell time column), and creative/campaign filter dropdowns. All follow existing shadcn/ui + Tailwind patterns. ANLYT-03 dwell time is visible across all display surfaces.
  </done>
</task>

<task type="auto">
  <name>Task 2: Analytics page composition and router wiring</name>
  <files>
    apps/web/src/features/analytics/pages/analytics-page.tsx
    apps/web/src/router.tsx
  </files>
  <action>
1. Create `apps/web/src/features/analytics/pages/analytics-page.tsx`:
   - Default export: `AnalyticsPage` component.
   - State: `datePreset` (DateRangePreset, default '30d'), `creativeId` (string | undefined), `campaignId` (string | undefined).
   - Derive `{ start, end }` from `getDateRange(datePreset)`.
   - Call `useAnalytics(start, end, { creativeId, campaignId })` for metrics data.
   - Call `useCreativeOptions()` and `useCampaignOptions()` for filter dropdowns.
   - Compute `chartData = useMemo(() => aggregateByDate(data ?? []), [data])` and `summary = useMemo(() => aggregateSummary(data ?? []), [data])`.
   - Layout (follow billing-page.tsx pattern with `space-y-8` wrapper):
     a. Page header: h1 "Analytics", subtitle "Track ad performance across campaigns and creatives".
     b. Controls row (`flex items-center justify-between gap-4`): DateRangeSelect on left, AnalyticsFilters in center, CsvExportButton on right.
     c. KpiCards with `impressions={summary.impressions}`, `clicks={summary.clicks}`, `ctr={summary.ctr}`, `engagements={summary.engagements}`, `totalDwellTimeMs={summary.totalDwellTimeMs}`, and loading state.
     d. MetricsChart with chartData and loading state.
     e. MetricsTable with raw data rows, creative/campaign options for name lookups, and loading state.
   - When data is empty and not loading, show an info message: "No analytics data for the selected period. Analytics appear once ads start serving."

2. Update `apps/web/src/router.tsx`:
   - Replace the `/analytics` route from `element: <SectionPlaceholder title="Analytics" phase={10} />` to a lazy-loaded route:
     ```typescript
     {
       path: '/analytics',
       lazy: async () => {
         const { default: Component } = await import(
           '@/features/analytics/pages/analytics-page'
         )
         return { Component }
       },
     },
     ```
   - Remove the SectionPlaceholder component if no other route uses it (check: only /analytics used it). If other routes still reference it, keep the function.

3. Verify the app builds: `cd apps/web && pnpm build` (Vite build should succeed).
  </action>
  <verify>
    - `apps/web/src/features/analytics/pages/analytics-page.tsx` exists with default export
    - `apps/web/src/router.tsx` has lazy-loaded /analytics route (no more SectionPlaceholder for analytics)
    - `cd apps/web && pnpm build` succeeds without errors
    - analytics-page.tsx passes `totalDwellTimeMs` to KpiCards (grep for "totalDwellTimeMs")
    - Visiting /analytics in the browser shows the analytics dashboard (not the Phase 10 placeholder)
  </verify>
  <done>
    Analytics dashboard page is live at /analytics with 5 KPI cards (including avg dwell time), time-series chart (with dwell time tab), metrics table (with avg dwell column), date range selection, creative/campaign filters, and CSV export (with dwell time column). Page refreshes trigger fresh data fetch (staleTime: 0). Today's data is included via automatic rollup_today_metrics() call. The SectionPlaceholder is replaced with the real dashboard. ANLYT-03 dwell time requirement fully satisfied across all display surfaces.
  </done>
</task>

</tasks>

<verification>
- Navigate to /analytics in the browser (after login)
- KPI cards show impressions, clicks, CTR, engagements, and avg dwell time (or zeros/"—" if no data)
- Chart renders with area visualization (or empty state message) and has 5 metric tabs including Dwell Time
- Date range dropdown changes the displayed data
- Creative and campaign filters narrow the results
- CSV export downloads a file with correct headers including Total Dwell Time (ms)
- Page refresh triggers fresh data load (network tab shows new request)
- Metrics table shows breakdown by creative and by campaign (toggle tabs) with Avg Dwell column
- `pnpm build` succeeds from apps/web
</verification>

<success_criteria>
- Analytics dashboard accessible at /analytics (no more placeholder)
- KPI cards display impressions, clicks, CTR, engagements, avg dwell time per ANLYT-01, ANLYT-02, ANLYT-03
- Time-series area chart with 5 metric tabs (including dwell time) per ANLYT-04
- Configurable date ranges via preset dropdown per ANLYT-04
- Fresh data on page refresh per ANLYT-05
- CSV export working with dwell time column per ANLYT-06
- Metrics table shows per-creative and per-campaign breakdowns with Avg Dwell column
- Creative and campaign filter dropdowns functional
- Dwell time displays "—" gracefully when value is 0 (historical rollups)
- Vite build passes
</success_criteria>

<output>
After completion, create `.planning/phases/10-analytics-reporting/10-02-SUMMARY.md`
</output>
