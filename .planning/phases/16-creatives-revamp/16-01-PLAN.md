---
phase: 16-creatives-revamp
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/router.tsx
  - apps/web/src/features/templates/pages/creatives-new-page.tsx
  - apps/web/src/features/creatives/pages/creatives-page.tsx
  - apps/web/src/features/creatives/components/creative-list.tsx
  - apps/web/src/features/dashboard/components/ad-type-card.tsx
  - apps/web/src/components/layout/search-dialog.tsx
autonomous: true
requirements:
  - CRV-01
  - CRV-02
  - CRV-03
  - CRV-04

must_haves:
  truths:
    - "Navigating to /creatives shows only the My Creatives list, never the format browser"
    - "Clicking 'Create New' from the Creatives list navigates to /creatives/new"
    - "Navigating to /creatives/new renders the 4-step Creatives browser (all 498 formats visible)"
    - "Dashboard ad-type card links point to /creatives/new?type={slug} not /creatives?type={slug}"
    - "Search dialog format links navigate to /creatives/new?type={slug} not /creatives?type=..."
  artifacts:
    - path: "apps/web/src/features/templates/pages/creatives-new-page.tsx"
      provides: "Thin page wrapper that renders <Creatives /> from creatives-selector.tsx"
      exports: ["default"]
    - path: "apps/web/src/router.tsx"
      provides: "Route wiring: /creatives/new -> creatives-new-page, /creatives -> creatives-page"
      contains: "creatives-new-page"
    - path: "apps/web/src/features/creatives/pages/creatives-page.tsx"
      provides: "Always renders <CreativeList />, no branching on query params"
    - path: "apps/web/src/features/creatives/components/creative-list.tsx"
      provides: "Create New and Browse Templates buttons navigate to /creatives/new"
    - path: "apps/web/src/features/dashboard/components/ad-type-card.tsx"
      provides: "Link href uses /creatives/new?type={slug}"
    - path: "apps/web/src/components/layout/search-dialog.tsx"
      provides: "Format links use /creatives/new?type={slug} instead of /creatives?type=...&format=..."
  key_links:
    - from: "apps/web/src/features/dashboard/components/ad-type-card.tsx"
      to: "/creatives/new?type={slug}"
      via: "Link to prop"
      pattern: "/creatives/new\\?type="
    - from: "apps/web/src/router.tsx"
      to: "apps/web/src/features/templates/pages/creatives-new-page.tsx"
      via: "lazy import"
      pattern: "creatives-new-page"
    - from: "apps/web/src/features/creatives/components/creative-list.tsx"
      to: "/creatives/new"
      via: "navigate() calls"
      pattern: "navigate\\('/creatives/new'\\)"
    - from: "apps/web/src/components/layout/search-dialog.tsx"
      to: "/creatives/new?type={slug}"
      via: "Link to prop"
      pattern: "/creatives/new\\?type="
---

<objective>
Restructure routing so /creatives always shows the Creatives list and /creatives/new hosts the 4-step format browser. Fix the dashboard card links, the "Create New" buttons, and the search dialog format links to point to the correct URLs.

Purpose: The current setup misroutes users — /creatives branches on query params to show the format browser, creating broken deep-link semantics. This plan establishes clean URL semantics before wiring category pre-selection in Plan 16-02.
Output: A thin `creatives-new-page.tsx`, a corrected router, a simplified creatives-page, two fixed navigate() calls in creative-list, updated dashboard card links, and a corrected search-dialog format link.
</objective>

<execution_context>
@C:/Users/HaseebRahman/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HaseebRahman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/web/src/router.tsx
@apps/web/src/features/creatives/pages/creatives-page.tsx
@apps/web/src/features/creatives/components/creative-list.tsx
@apps/web/src/features/dashboard/components/ad-type-card.tsx
@apps/web/src/features/templates/pages/creatives-selector.tsx
@apps/web/src/features/templates/styles/creatives.css
@apps/web/src/components/layout/search-dialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create creatives-new-page and update router</name>
  <files>
    apps/web/src/features/templates/pages/creatives-new-page.tsx
    apps/web/src/router.tsx
  </files>
  <action>
**Create `apps/web/src/features/templates/pages/creatives-new-page.tsx`:**

This is a thin page wrapper. It imports and renders the default export from `./creatives-selector` (the `Creatives` component). Wrap the component in a `div` with `className="mt-0"` to override the `margin-top: 84px` rule in `creatives.css` that targets `.cr-root` (that margin was designed for a fixed top-nav that does not exist in this app shell). The component receives no props at this stage — category pre-selection is wired in Plan 16-02.

```tsx
import Creatives from './creatives-selector'

export default function CreativesNewPage() {
  return (
    <div className="mt-0">
      <Creatives />
    </div>
  )
}
```

**Modify `apps/web/src/router.tsx`:**

The `/creatives/new` route currently imports `editor-page`. Change the lazy import target to `@/features/templates/pages/creatives-new-page` instead. No other routes change — `/creatives` still points to `creatives-page`, and `/creatives/:id/edit` still points to `editor-page`.

The updated `/creatives/new` route block becomes:
```ts
{
  path: '/creatives/new',
  lazy: async () => {
    const { default: Component } = await import(
      '@/features/templates/pages/creatives-new-page'
    )
    return { Component }
  },
},
```
  </action>
  <verify>Run `pnpm --filter web build` (or `pnpm --filter @scrolltoday/web build`) from the repo root and confirm zero TypeScript/build errors. Alternatively run `pnpm tsc --noEmit` inside `apps/web/`.</verify>
  <done>Build completes without errors. `creatives-new-page.tsx` exists. `router.tsx` imports `creatives-new-page` for the `/creatives/new` path.</done>
</task>

<task type="auto">
  <name>Task 2: Simplify creatives-page and fix navigation targets</name>
  <files>
    apps/web/src/features/creatives/pages/creatives-page.tsx
    apps/web/src/features/creatives/components/creative-list.tsx
    apps/web/src/features/dashboard/components/ad-type-card.tsx
  </files>
  <action>
**Modify `apps/web/src/features/creatives/pages/creatives-page.tsx`:**

Remove the `useSearchParams` branch entirely. The page must always render `<CreativeList />` — no conditional logic. Remove the `TemplatesPage` import and the `useSearchParams` import too (they are now unused).

Final file content:
```tsx
import { CreativeList } from '../components/creative-list'

export default function CreativesPage() {
  return <CreativeList />
}
```

**Modify `apps/web/src/features/creatives/components/creative-list.tsx`:**

There are exactly two `navigate('/creatives?type=all')` calls in this file:
1. The "Create New" button's `onClick` handler (line ~131)
2. The "Browse Templates" button's `onClick` handler in the empty state (line ~161)

Change both to `navigate('/creatives/new')`. No other changes to this file.

**Modify `apps/web/src/features/dashboard/components/ad-type-card.tsx`:**

The `Link` component currently uses `to={\`/creatives?type=${adType.slug}\`}`. Change it to `to={\`/creatives/new?type=${adType.slug}\`}`. This is a one-character change to the path prefix. No other changes.
  </action>
  <verify>
1. Run `pnpm tsc --noEmit` inside `apps/web/` — must pass with zero errors.
2. Grep confirms the old URLs are gone: `grep -r "creatives?type=" apps/web/src/` should return zero results.
3. Grep confirms the new URLs: `grep -r "creatives/new" apps/web/src/` should show hits in router.tsx, creative-list.tsx (x2), and ad-type-card.tsx.
  </verify>
  <done>
- `creatives-page.tsx` has no imports or references to TemplatesPage or useSearchParams.
- Both navigate() calls in `creative-list.tsx` target `/creatives/new`.
- `ad-type-card.tsx` Link target is `/creatives/new?type=${adType.slug}`.
- TypeScript build passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix search-dialog format link URL</name>
  <files>
    apps/web/src/components/layout/search-dialog.tsx
  </files>
  <action>
**Modify `apps/web/src/components/layout/search-dialog.tsx`:**

Around line 103, there is a Link (or anchor) that navigates to format search results using a URL of this form:

```
`/creatives?type=${type.slug}&format=${format.slug}`
```

Change this to:

```
`/creatives/new?type=${type.slug}`
```

Drop the `&format=${format.slug}` segment entirely — the `?format=` param was consumed by the old branching logic in `creatives-page.tsx` which no longer exists. The `?type=` param is still valid and will be picked up by `creatives-new-page.tsx` in Plan 16-02 to pre-select the correct category.

This is a targeted single-line change. Do NOT alter any other logic, imports, or JSX in this file.
  </action>
  <verify>
1. Run `pnpm tsc --noEmit` inside `apps/web/` — must pass with zero errors.
2. `grep -n "creatives?type=" apps/web/src/components/layout/search-dialog.tsx` returns zero results.
3. `grep -n "creatives/new" apps/web/src/components/layout/search-dialog.tsx` shows one hit containing `?type=`.
  </verify>
  <done>
- `search-dialog.tsx` line 103 navigates to `/creatives/new?type=${type.slug}` (no `&format=` segment).
- No other changes to the file.
- TypeScript build passes.
  </done>
</task>

</tasks>

<verification>
After all three tasks complete:
1. `pnpm --filter web build` exits 0 (no TS errors, no missing imports).
2. `grep -r "creatives?type=" apps/web/src/` returns zero results (old URL fully removed from all files).
3. `grep -rn "creatives/new" apps/web/src/router.tsx` shows the new route import.
4. `apps/web/src/features/templates/pages/creatives-new-page.tsx` exists and exports a default component.
5. `grep -n "creatives/new" apps/web/src/components/layout/search-dialog.tsx` shows one hit with `?type=` and no `&format=`.
</verification>

<success_criteria>
- `/creatives/new` route is served by `creatives-new-page.tsx` (wraps the 498-format Creatives browser).
- `/creatives` route is served by `creatives-page.tsx` which unconditionally renders `<CreativeList />`.
- Dashboard Ad Type cards link to `/creatives/new?type={slug}`.
- "Create New" and "Browse Templates" buttons in `creative-list.tsx` navigate to `/creatives/new`.
- Search dialog format links navigate to `/creatives/new?type={slug}` (no `&format=` param).
- Zero TypeScript errors.
</success_criteria>

<output>
After completion, create `.planning/phases/16-creatives-revamp/16-01-SUMMARY.md` using the summary template.
</output>
