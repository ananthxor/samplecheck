---
phase: 06-animated-video-standard-native
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/features/templates/formats/static-banner/renderer.ts
  - apps/web/src/features/templates/formats/multi-frame/renderer.ts
  - apps/web/src/features/templates/formats/in-feed/renderer.ts
autonomous: true

must_haves:
  truths:
    - "Static banner renders image, headline, body text, and CTA button with configurable colors, and CTA has a hover state"
    - "Multi-frame banner auto-rotates through frames with cross-fade transition, driven by frameDuration config"
    - "In-feed native ad renders sponsor bar, content image, headline, body text, CTA link, and a 'Sponsored' disclosure badge"
  artifacts:
    - path: "apps/web/src/features/templates/formats/static-banner/renderer.ts"
      provides: "Static banner renderer with CTA hover state"
      contains: "sb-cta:hover"
    - path: "apps/web/src/features/templates/formats/multi-frame/renderer.ts"
      provides: "Multi-frame auto-rotation renderer with frameDuration"
      contains: "cfg.frameDuration"
    - path: "apps/web/src/features/templates/formats/in-feed/renderer.ts"
      provides: "In-feed renderer with Sponsored disclosure badge"
      contains: "Sponsored"
  key_links:
    - from: "multi-frame/renderer.ts"
      to: "renderer-shell.ts frameTimer"
      via: "frameTimer = setInterval for auto-rotation (cleared on re-render by shell)"
      pattern: "frameTimer = setInterval"
    - from: "static-banner/renderer.ts"
      to: "renderer-shell.ts"
      via: "RendererExport CSS/JS injected into buildPreviewHtml"
      pattern: "renderStaticBanner"
---

<objective>
Polish the three standard/native format renderers (static-banner, multi-frame, in-feed) that are already substantially complete from Phase 4.

Purpose: These three formats already render correctly but need minor enhancements for production quality: CTA hover state on static banner, confirmation that frameDuration config is properly wired in multi-frame, and a "Sponsored" disclosure badge on in-feed per IAB native ad guidelines. These are small, focused improvements to existing working renderers.

Output: Three polished standard/native format renderers ready for production use.
</objective>

<execution_context>
@C:/Users/HaseebRahman/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HaseebRahman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/web/src/features/templates/formats/static-banner/renderer.ts
@apps/web/src/features/templates/formats/static-banner/config.ts
@apps/web/src/features/templates/formats/multi-frame/renderer.ts
@apps/web/src/features/templates/formats/multi-frame/config.ts
@apps/web/src/features/templates/formats/in-feed/renderer.ts
@apps/web/src/features/templates/formats/in-feed/config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish static banner with CTA hover state and minor improvements</name>
  <files>apps/web/src/features/templates/formats/static-banner/renderer.ts</files>
  <action>
Update the `staticBannerRenderer` CSS section to add a hover state for the CTA button. The current renderer works correctly but the CTA has no visual feedback on hover.

**CSS changes** -- add after the existing `.sb-container p` rule:
- `.sb-cta:hover`: `opacity: 0.9; transform: translateY(-1px); transition: opacity 0.2s, transform 0.2s;`
- Also add `transition: opacity 0.2s, transform 0.2s;` to the shared `.sb-cta` class in renderer-shell.ts... actually NO -- the `.sb-cta` class is defined in renderer-shell.ts and shared across all formats. Instead, add the transition and hover to the static banner's own CSS using the scoped selector `.sb-container .sb-cta` for just this format's CTA:
  - `.sb-container .sb-cta`: `transition: opacity 0.2s ease, transform 0.2s ease;`
  - `.sb-container .sb-cta:hover`: `opacity: 0.9; transform: translateY(-1px);`

Wait -- actually the `.sb-cta` class is shared across formats (defined in renderer-shell.ts). Adding hover state in static-banner's CSS is fine since it's scoped to `sb-container` context. But for consistency across ALL formats, it would be better to add the hover to the shared `.sb-cta` definition in renderer-shell.ts. However, renderer-shell.ts is owned by Plan 02 in this phase. To avoid file conflicts:

Add the hover styles using the more specific `.sb-container .sb-cta` selector in static-banner's own CSS section:
```css
.sb-container .sb-cta { transition: opacity 0.2s ease, transform 0.2s ease; }
.sb-container .sb-cta:hover { opacity: 0.9; transform: translateY(-1px); }
```

**JS section** -- no changes needed. The current renderStaticBanner function is complete and correct.

The static banner renderer is already fully functional. This task adds only the CSS hover enhancement.
  </action>
  <verify>
Run `npx tsc --noEmit` from `apps/web/`. Confirm CSS contains `.sb-container .sb-cta:hover` with opacity and transform properties.
  </verify>
  <done>
Static banner CTA button has hover state with subtle opacity change and upward translate, providing visual feedback on interaction.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify multi-frame renderer and add dot indicators + CTA hover</name>
  <files>apps/web/src/features/templates/formats/multi-frame/renderer.ts</files>
  <action>
The multi-frame renderer is already substantially complete with auto-rotation and cross-fade transitions. Enhance it with:

**CSS additions** -- add after existing styles:
- `.mf-dots`: `position: absolute; bottom: 8px; left: 50%; transform: translateX(-50%); display: flex; gap: 6px; z-index: 5;`
- `.mf-dot`: `width: 8px; height: 8px; border-radius: 50%; background: rgba(255,255,255,0.5); transition: background 0.3s;`
- `.mf-dot.active`: `background: rgba(255,255,255,0.95);`

**JS changes** -- after the closing `</div>` for frames and before the CTA button, add a dot indicator row if `frames.length > 1`:
```javascript
if (frames.length > 1) {
  html += '<div class="mf-dots">';
  for (var d = 0; d < frames.length; d++) {
    html += '<div class="mf-dot' + (d === 0 ? ' active' : '') + '" data-dot="' + d + '"></div>';
  }
  html += '</div>';
}
```

In the setInterval callback, after updating the active frame class, also update dots:
```javascript
var dots = root.querySelectorAll('.mf-dot');
if (dots.length > 0) {
  dots[current === 0 ? frames.length - 1 : current - 1].classList.remove('active');
  // Actually, simpler: just update all dots
  for (var di = 0; di < dots.length; di++) {
    dots[di].classList.remove('active');
  }
  dots[current].classList.add('active');
}
```

Wait -- the current implementation uses `allFrames[current].classList.remove('active')` then increments current. The cleaner approach:

Inside the existing setInterval callback, after `allFrames[current].classList.add('active');`, add dot update logic:
```javascript
var allDots = root.querySelectorAll('.mf-dot');
for (var di = 0; di < allDots.length; di++) {
  allDots[di].classList.remove('active');
}
if (allDots[current]) { allDots[current].classList.add('active'); }
```

The `cfg.frameDuration` config field is already wired: `cfg.frameDuration || 3000` in the existing setInterval call. Confirm this remains intact.

This task preserves all existing multi-frame functionality and adds visual dot indicators to show which frame is active.
  </action>
  <verify>
Run `npx tsc --noEmit` from `apps/web/`. Confirm CSS contains `.mf-dots`, `.mf-dot`, `.mf-dot.active`. Confirm JS contains `mf-dot` creation loop and dot update in setInterval callback. Confirm `cfg.frameDuration || 3000` is still used in setInterval.
  </verify>
  <done>
Multi-frame renderer has dot indicators showing active frame position, with dots updating in sync with auto-rotation. frameDuration config is confirmed wired. All existing cross-fade and auto-rotation functionality preserved.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Sponsored disclosure badge to in-feed renderer</name>
  <files>apps/web/src/features/templates/formats/in-feed/renderer.ts</files>
  <action>
The in-feed renderer is already substantially complete with sponsor bar, image, body, and CTA. Add a "Sponsored" disclosure badge per IAB native ad guidelines and add CTA hover state.

**CSS additions** -- add after existing styles:
- `.if-badge`: `position: absolute; top: 8px; right: 8px; background: rgba(0,0,0,0.6); color: #fff; font-size: 10px; padding: 2px 6px; border-radius: 3px; text-transform: uppercase; letter-spacing: 0.5px; z-index: 2;`
- `.if-card`: add `position: relative;` to existing `.if-card` rule (needed for absolute badge positioning)
- `.if-cta:hover`: `text-decoration: underline;`

**JS changes** -- in `renderInFeed(root, cfg)`:
After the opening `<div class="if-card">`, add a "Sponsored" badge:
```javascript
'<div class="if-badge">Sponsored</div>'
```

This badge appears in the top-right corner of the card regardless of whether a sponsor name/logo is provided, since IAB guidelines require clear "Sponsored" labeling on all native ads.

All existing functionality (sponsor bar with logo, content image, headline, body text, CTA link) must be preserved. The badge is purely additive.
  </action>
  <verify>
Run `npx tsc --noEmit` from `apps/web/`. Confirm CSS contains `.if-badge` with "position: absolute". Confirm JS HTML string contains "Sponsored" badge div. Confirm existing sponsor bar, image, body, and CTA link HTML generation is unchanged.
  </verify>
  <done>
In-feed native ad renderer has IAB-compliant "Sponsored" disclosure badge in top-right corner and CTA hover underline. All existing layout (sponsor bar, image, body, CTA) preserved.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` from `apps/web/` passes with zero errors
2. `pnpm run build` from monorepo root succeeds
3. static-banner has CTA hover state CSS
4. multi-frame has dot indicators that sync with auto-rotation
5. in-feed has "Sponsored" disclosure badge
</verification>

<success_criteria>
- Static banner CTA has visual hover feedback
- Multi-frame banner shows dot indicators that update with frame rotation
- In-feed native ad displays "Sponsored" badge per IAB native ad guidelines
- All three renderers continue to work correctly within the existing renderer architecture
- No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-animated-video-standard-native/06-03-SUMMARY.md`
</output>
