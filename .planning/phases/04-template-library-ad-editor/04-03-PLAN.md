---
phase: 04-template-library-ad-editor
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - supabase/migrations/20260220000000_add_preview_token.sql
  - packages/shared/src/database.types.ts
  - apps/web/src/features/creatives/components/creative-list.tsx
  - apps/web/src/features/creatives/components/creative-card.tsx
  - apps/web/src/features/creatives/components/creative-actions.tsx
  - apps/web/src/features/creatives/components/share-dialog.tsx
  - apps/web/src/features/creatives/pages/creatives-page.tsx
  - apps/web/src/features/preview/pages/preview-page.tsx
  - apps/web/src/router.tsx
autonomous: false

must_haves:
  truths:
    - "User can view all saved creatives in My Creatives library with status and thumbnail"
    - "User can generate a shareable preview link for any saved creative"
    - "Shareable preview link opens in a new browser without requiring login"
    - "User can edit, delete, and share creatives from the library"
    - "/creatives shows My Creatives when no type/format params, shows templates when params are present"
  artifacts:
    - path: "supabase/migrations/20260220000000_add_preview_token.sql"
      provides: "preview_token column and anon RLS policy on creatives"
      contains: "preview_token"
    - path: "apps/web/src/features/creatives/pages/creatives-page.tsx"
      provides: "Dual-mode page: template browsing or My Creatives library"
    - path: "apps/web/src/features/creatives/components/creative-list.tsx"
      provides: "Grid/list of user's saved creatives"
    - path: "apps/web/src/features/creatives/components/share-dialog.tsx"
      provides: "Dialog for generating and copying preview link"
      contains: "preview_token"
    - path: "apps/web/src/features/preview/pages/preview-page.tsx"
      provides: "Public preview page accessible without authentication"
  key_links:
    - from: "apps/web/src/features/creatives/components/share-dialog.tsx"
      to: "apps/web/src/features/creatives/api/creatives-api.ts"
      via: "updateCreative to set preview_token"
      pattern: "updateCreative.*preview_token"
    - from: "apps/web/src/features/preview/pages/preview-page.tsx"
      to: "apps/web/src/features/creatives/api/creatives-api.ts"
      via: "fetchCreativeByToken for public access"
      pattern: "fetchCreativeByToken"
    - from: "apps/web/src/router.tsx"
      to: "apps/web/src/features/preview/pages/preview-page.tsx"
      via: "public route at /preview/:token outside ProtectedRoute"
      pattern: "/preview/:token"
    - from: "supabase/migrations/20260220000000_add_preview_token.sql"
      to: "packages/shared/src/database.types.ts"
      via: "preview_token column reflected in TypeScript types"
      pattern: "preview_token"
---

<objective>
Build the My Creatives library page, the shareable preview link system (including database migration for preview_token), and the public preview page. Convert the /creatives route to dual-mode: template browsing when type/format params present, My Creatives library otherwise.

Purpose: Completes the creative lifecycle: browse templates -> customize -> save -> view in library -> share preview link. The shareable preview link enables non-logged-in stakeholders to review creatives, which is essential for the advertiser collaboration workflow.

Output: My Creatives library at /creatives, share dialog with copyable preview URL, public preview page at /preview/:token, database migration for preview_token column with anon RLS.
</objective>

<execution_context>
@C:/Users/HaseebRahman/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HaseebRahman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-template-library-ad-editor/04-RESEARCH.md
@.planning/phases/04-template-library-ad-editor/04-01-SUMMARY.md
@.planning/phases/04-template-library-ad-editor/04-02-SUMMARY.md

Key existing files:
@apps/web/src/features/creatives/api/creatives-api.ts
@apps/web/src/features/creatives/hooks/use-creatives.ts
@apps/web/src/features/editor/lib/renderer.ts
@apps/web/src/features/templates/pages/templates-page.tsx
@apps/web/src/router.tsx
@apps/web/src/lib/supabase.ts
@packages/shared/src/database.types.ts
@supabase/migrations/20260219000000_initial_schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database migration for preview_token, type update, My Creatives library components</name>
  <files>
    supabase/migrations/20260220000000_add_preview_token.sql
    packages/shared/src/database.types.ts
    apps/web/src/features/creatives/components/creative-list.tsx
    apps/web/src/features/creatives/components/creative-card.tsx
    apps/web/src/features/creatives/components/creative-actions.tsx
    apps/web/src/features/creatives/pages/creatives-page.tsx
  </files>
  <action>
    **Step 1: Create migration file** at `supabase/migrations/20260220000000_add_preview_token.sql`:

    ```sql
    -- Add preview_token column for shareable preview links
    ALTER TABLE public.creatives ADD COLUMN preview_token TEXT UNIQUE;

    -- Partial index for fast token lookups (only index non-null tokens)
    CREATE INDEX idx_creatives_preview_token ON public.creatives (preview_token) WHERE preview_token IS NOT NULL;

    -- RLS policy: Allow anonymous (unauthenticated) users to read creatives by preview_token
    -- This enables shareable preview links to work without login
    -- The application always queries with .eq('preview_token', token) so only the matching row is returned
    CREATE POLICY "Public preview access by token"
    ON public.creatives FOR SELECT
    TO anon
    USING (preview_token IS NOT NULL);
    ```

    **Step 2: Push migration to Supabase:**
    ```bash
    npx supabase db push --db-url "$DB_URL"
    ```
    Use the same `--db-url` pattern from Phase 1 (read the connection string from `.env.development` VITE_SUPABASE_URL, converting it to the direct postgres connection format: `postgresql://postgres.[ref]:[password]@aws-0-ap-south-1.pooler.supabase.com:5432/postgres`). The actual DB URL should be constructed from the existing env vars or use the direct connection string from Phase 1 decision.

    If `supabase db push --db-url` is unavailable or fails, run the SQL directly via Supabase Dashboard SQL Editor as a fallback.

    **Step 3: Update TypeScript types** in `packages/shared/src/database.types.ts`:

    Add `preview_token: string | null` to the `creatives` table types:
    - `Row`: add `preview_token: string | null`
    - `Insert`: add `preview_token?: string | null`
    - `Update`: add `preview_token?: string | null`

    Place the field alphabetically among existing fields in each type block.

    **Step 4: Create `creative-card.tsx`** at `apps/web/src/features/creatives/components/creative-card.tsx`:

    A card component for a single creative in the library. Props: `creative: Tables<'creatives'>, onEdit: (id: string) => void, onShare: (creative: Tables<'creatives'>) => void, onDelete: (id: string) => void`.

    Layout (using shadcn Card):
    - Top: Thumbnail area. If `thumbnail_url` exists, show it. Otherwise, show a gradient placeholder with format name.
    - Title row: creative name (font-semibold, truncate), status badge (using shadcn Badge -- draft=secondary, active=default, paused=outline, archived=destructive)
    - Subtitle: format_name, dimensions (width x height), "Updated {relative time}" (use simple date formatting, no need for a library)
    - Bottom row: action buttons using CreativeActions component

    **Step 5: Create `creative-actions.tsx`** at `apps/web/src/features/creatives/components/creative-actions.tsx`:

    An actions dropdown for a creative. Props: `creativeId: string, creative: Tables<'creatives'>, onEdit: (id: string) => void, onShare: (creative: Tables<'creatives'>) => void, onDelete: (id: string) => void`.

    Use shadcn DropdownMenu (already installed from Phase 3):
    - Trigger: ellipsis (MoreHorizontal) icon button
    - Items:
      - "Edit" (Pencil icon) -> calls onEdit(creativeId)
      - "Preview" (Eye icon) -> opens `/creatives/${creativeId}/edit` in same tab (or could reuse editor in view mode, but for now just opens editor)
      - "Share" (Share2 icon) -> calls onShare(creative)
      - Separator
      - "Delete" (Trash2 icon, text-destructive) -> shows shadcn AlertDialog confirmation, then calls onDelete(creativeId)

    The delete flow uses shadcn AlertDialog (installed in Task 1 of Plan 01) with title "Delete Creative", description "This action cannot be undone. This will permanently delete the creative.", and Cancel/Delete buttons.

    **Step 6: Create `creative-list.tsx`** at `apps/web/src/features/creatives/components/creative-list.tsx`:

    The main My Creatives library component. No props (fetches its own data).

    Implementation:
    - Use `useCreatives()` hook to fetch all creatives
    - Use `useDeleteCreative()` for delete operations
    - State: `shareCreative: Tables<'creatives'> | null` for controlling the share dialog (Plan 03 Task 2)
    - Layout:
      - Header row: "My Creatives" title, "Create New" button (navigates to /creatives?type=all or just the template browsing flow)
      - Responsive grid: 1 col mobile, 2 cols md, 3 cols lg
      - Each creative rendered as `CreativeCard`
      - Loading state: skeleton cards (3-6 skeleton placeholders)
      - Empty state: centered illustration-free message "No creatives yet" with CTA button "Browse Templates" linking to /creatives?type=all
      - Error state: error message with retry button
    - onEdit handler: navigate to `/creatives/${id}/edit`
    - onShare handler: set shareCreative state (opens ShareDialog)
    - onDelete handler: call deleteCreative mutation, show toast on success/error

    Render `ShareDialog` at the bottom (conditionally, when shareCreative is set). Pass `open`, `onOpenChange`, `creativeId`, and `existingToken` from shareCreative.preview_token.

    **Step 7: Create dual-mode `creatives-page.tsx`** at `apps/web/src/features/creatives/pages/creatives-page.tsx`:

    This page replaces the current direct import of templates-page at `/creatives`. It checks URL params to determine which view to show:

    ```tsx
    import { useSearchParams } from 'react-router'
    import { TemplatesPage } from '@/features/templates/pages/templates-page'
    import { CreativeList } from '../components/creative-list'

    export default function CreativesPage() {
      const [searchParams] = useSearchParams()
      const typeFilter = searchParams.get('type')
      const formatFilter = searchParams.get('format')

      // If type/format params present, show template browsing
      // Otherwise show My Creatives library
      if (typeFilter || formatFilter) {
        return <TemplatesPage type={typeFilter} format={formatFilter} />
      }

      return <CreativeList />
    }
    ```

    **Step 8: Update `router.tsx`**:

    Change the `/creatives` route to import the new dual-mode creatives-page instead of templates-page:
    ```tsx
    {
      path: '/creatives',
      lazy: async () => {
        const { default: Component } = await import(
          '@/features/creatives/pages/creatives-page'
        )
        return { Component }
      },
    },
    ```

    Also update `TemplatesPage` to accept props (type, format) instead of reading from URL params directly, since it's now a child component. Alternatively, `TemplatesPage` can continue reading from URL params since the parent just conditionally renders it and the params are still in the URL. Choose the simpler approach (keep reading from URL params in TemplatesPage).
  </action>
  <verify>
    1. Run migration: verify `preview_token` column exists on creatives table via `SELECT column_name FROM information_schema.columns WHERE table_name = 'creatives' AND column_name = 'preview_token'`
    2. Run `cd apps/web && pnpm exec tsc --noEmit` -- no type errors
    3. Navigate to `/creatives` (no params) -- shows My Creatives library (empty state initially)
    4. Navigate to `/creatives?type=standard` -- shows template browsing filtered to standard type
    5. Create a creative via the editor, then navigate to `/creatives` -- creative appears in the library with correct name, format, status badge
    6. Click delete on a creative -- confirmation dialog appears, deleting removes it from the list
  </verify>
  <done>Migration deployed with preview_token column and anon RLS policy. TypeScript types updated. My Creatives library shows all saved creatives with status badges and action menus. /creatives route is dual-mode (library vs template browsing). Delete with confirmation works.</done>
</task>

<task type="auto">
  <name>Task 2: Share dialog, public preview page, and router wiring for shareable links</name>
  <files>
    apps/web/src/features/creatives/components/share-dialog.tsx
    apps/web/src/features/preview/pages/preview-page.tsx
    apps/web/src/router.tsx
  </files>
  <action>
    **Step 1: Create `share-dialog.tsx`** at `apps/web/src/features/creatives/components/share-dialog.tsx`:

    Props: `open: boolean, onOpenChange: (open: boolean) => void, creativeId: string, existingToken: string | null`.

    Implementation:
    - Use shadcn Dialog (already installed from Phase 3)
    - State: `token` (initialized from existingToken), `copied` (boolean), `isGenerating` (boolean)
    - Compute `previewUrl`: `${window.location.origin}/preview/${token}` when token exists
    - "Generate Preview Link" button:
      1. Generate UUID via `crypto.randomUUID()`
      2. Call `updateCreative(creativeId, { preview_token: newToken })` (import from creatives-api)
      3. Set token state, show success toast
    - If token already exists (link was previously generated), show the URL directly with a copy button
    - Copy button: uses `navigator.clipboard.writeText(previewUrl)`, shows Check icon for 2 seconds after copy
    - Layout:
      - Dialog header: "Share Preview" title, "Anyone with this link can view the creative without logging in." description
      - If URL exists: flex row with readonly Input showing URL + copy icon Button
      - If no URL: "Generate Preview Link" button (disabled while generating)
    - When generating a new token for a creative that already has one, replace the old token (regenerate)

    Also invalidate the creatives query cache after generating a token so the library reflects the new preview_token.

    **Step 2: Create `preview-page.tsx`** at `apps/web/src/features/preview/pages/preview-page.tsx`:

    Public page at `/preview/:token` -- accessible WITHOUT authentication.

    Implementation:
    - Read `token` from route params via `useParams()`
    - Fetch creative via `fetchCreativeByToken(token)` -- this uses the Supabase anon key (no auth needed thanks to the anon RLS policy)
    - Use `useQuery` directly for the fetch (not `useCreative` which may have auth assumptions):
      ```tsx
      const { data: creative, isLoading, error } = useQuery({
        queryKey: ['preview', token],
        queryFn: () => fetchCreativeByToken(token!),
        enabled: !!token,
      })
      ```
    - Once creative is loaded:
      1. Parse `template_data` as the config (it's stored as JSONB, comes back as an object)
      2. Generate preview HTML via `generatePreviewHtml(config as TemplateConfig)`
      3. Render full-page iframe with srcdoc
    - Layout:
      - Full viewport: `h-screen w-screen bg-white`
      - Centered iframe matching the creative's width/height (or responsive if not set)
      - No header, no sidebar, no navigation (this is a standalone preview page)
      - Small "Powered by ScrollToday" footer link at bottom (subtle, text-xs, text-muted-foreground)
    - Loading state: centered Loader2 spinner
    - Error state: "Preview not found" message with "This preview link may be invalid or expired." subtext
    - If token is missing: redirect to / or show error

    IMPORTANT: This page must work WITHOUT AuthProvider/useAuth. The Supabase client singleton uses the anon key by default. The `fetchCreativeByToken` function doesn't need authentication -- the anon RLS policy allows SELECT by preview_token.

    **Step 3: Update `router.tsx`**:

    Add the public preview route OUTSIDE the ProtectedRoute wrapper. Place it at the top level, alongside `/login`:

    ```tsx
    export const router = createBrowserRouter([
      {
        path: '/preview/:token',
        lazy: async () => {
          const { default: Component } = await import(
            '@/features/preview/pages/preview-page'
          )
          return { Component }
        },
      },
      {
        path: '/login',
        // ... existing
      },
      {
        element: <ProtectedRoute />,
        // ... existing children
      },
    ])
    ```

    CRITICAL: The `/preview/:token` route MUST be outside `ProtectedRoute`. If placed inside, unauthenticated users will be redirected to login, defeating the purpose of shareable links.

    Also ensure the preview page is wrapped in a `QueryClientProvider` if it needs TanStack Query. Check if `QueryClientProvider` wraps the entire app (in main.tsx or App.tsx). If yes, the preview page has access. If the QueryClientProvider is inside ProtectedRoute, the preview page won't have it -- in that case, add a minimal QueryClientProvider wrapper in the preview page or restructure the provider tree.

    Check the app's provider structure and ensure the preview page has access to QueryClientProvider. If needed, the preview page can create its own QueryClient instance or use raw fetch instead of TanStack Query.
  </action>
  <verify>
    1. Run `cd apps/web && pnpm exec tsc --noEmit` -- no type errors
    2. Run `cd apps/web && pnpm run build` -- clean production build
    3. Full share flow: create a creative -> go to My Creatives -> click Share -> "Generate Preview Link" -> copy URL -> open in incognito/new browser -> preview renders the creative correctly
    4. Verify preview works without login: open incognito window, paste preview URL, creative renders
    5. Verify share dialog shows existing token URL if creative already has one
    6. Verify copy button works (clipboard API)
  </verify>
  <done>Share dialog generates preview_token and constructs shareable URL. Public preview page renders creative without authentication via anon RLS policy. URL is copyable. Preview route is outside ProtectedRoute.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT - Complete Phase 4 verification</name>
  <files>None (verification only)</files>
  <action>
    User verifies the complete Phase 4 implementation across 5 test flows.

    **What was built:**
    1. Template browsing with 20 templates across 14 formats, filtered by ad type/format
    2. Split-pane ad editor with live iframe preview, device mode toggle, image upload
    3. Save-to-library workflow creating creatives in Supabase
    4. My Creatives library with status badges, edit/delete/share actions
    5. Shareable preview links accessible without login
  </action>
  <verify>
    **Flow 1: Template Browsing**
    1. Login and go to Dashboard
    2. Click any ad type card (e.g., "Standard") -> should navigate to /creatives?type=standard showing only standard templates
    3. Use the ad type filter dropdown to switch to "Interactive" -> should show interactive format templates
    4. Use the format filter to narrow to "Swipeable Carousel" -> should show carousel templates only
    5. Verify 20 total templates when no filters applied

    **Flow 2: Creative Editor**
    6. Click any static-banner template -> editor opens at /creatives/new?template=X
    7. Left panel shows tabbed form (Content, Style, Settings), right panel shows live preview
    8. Type a new headline -> preview updates in real-time (no flash)
    9. Change background color -> preview updates
    10. Toggle between desktop and mobile preview -> preview container resizes
    11. Click Save -> toast confirms save, URL updates to /creatives/{id}/edit

    **Flow 3: My Creatives Library**
    12. Navigate to /creatives (sidebar "Creatives" link) -> shows My Creatives with the saved creative
    13. Creative card shows name, format, status "draft", dimensions
    14. Click the actions menu -> Edit, Share, Delete options visible

    **Flow 4: Shareable Preview**
    15. Click "Share" on a creative -> share dialog opens
    16. Click "Generate Preview Link" -> URL appears
    17. Copy the URL
    18. Open it in an incognito/private browser window -> creative preview renders without login
    19. Verify the preview matches what was in the editor

    **Flow 5: Edit Existing**
    20. From My Creatives, click "Edit" on a creative -> editor opens with saved data
    21. Make changes -> Save -> verify changes persist

    Approve if all 5 flows work. Report issues if any flow fails.
  </verify>
  <done>User approves all 5 flows: template browsing, creative editor, My Creatives library, shareable preview, and edit existing. Phase 4 success criteria are met.</done>
</task>

</tasks>

<verification>
1. `pnpm exec tsc --noEmit` and `pnpm run build` pass cleanly
2. Full lifecycle: browse templates -> select -> customize in editor -> save -> appears in library -> share -> preview works without login
3. /creatives dual mode: no params = My Creatives, with params = template browsing
4. preview_token column exists in database with anon RLS policy
5. Public preview route is outside ProtectedRoute
6. Delete with confirmation removes creative from library and database
</verification>

<success_criteria>
- My Creatives library shows all user's saved creatives with status badges and actions
- Share dialog generates a preview_token UUID and constructs a copyable preview URL
- Public preview page at /preview/:token renders the creative without authentication
- /creatives is dual-mode: library (no params) or template browsing (with type/format params)
- Database migration deployed with preview_token column and anon RLS policy
- All 5 Phase 4 success criteria are met (browse templates, customize, device toggle, save to library, shareable preview)
</success_criteria>

<output>
After completion, create `.planning/phases/04-template-library-ad-editor/04-03-SUMMARY.md`
</output>
