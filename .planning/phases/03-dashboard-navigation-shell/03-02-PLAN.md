---
phase: 03-dashboard-navigation-shell
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - apps/web/src/features/dashboard/data/ad-types.ts
  - apps/web/src/features/dashboard/components/ad-type-card.tsx
  - apps/web/src/features/dashboard/components/ad-type-grid.tsx
  - apps/web/src/features/dashboard/components/platform-suite-section.tsx
  - apps/web/src/features/dashboard/pages/dashboard-page.tsx
  - apps/web/src/components/layout/search-dialog.tsx
  - apps/web/src/components/layout/app-shell.tsx
  - apps/web/src/components/layout/app-header.tsx
  - apps/web/src/router.tsx
autonomous: true

must_haves:
  truths:
    - "Dashboard page shows a responsive grid of ad type cards (Interactive, Animated, Video, Standard, Native) that users can click to browse templates for that type"
    - "Each ad type card shows the type name, icon, format count badge, description, and up to 3 format names with a +N more indicator"
    - "Platform Suite section below ad type cards shows placeholder cards for Audio, ADCTV, and Social Display with 'Coming Soon' treatment"
    - "Pressing Ctrl+K (or Cmd+K on Mac) opens a search dialog with results across ad formats and platform sections"
    - "Selecting a search result navigates to the corresponding route"
  artifacts:
    - path: "apps/web/src/features/dashboard/data/ad-types.ts"
      provides: "Static ad type and format definitions, Platform Suite items"
      contains: "AD_TYPES"
    - path: "apps/web/src/features/dashboard/pages/dashboard-page.tsx"
      provides: "Dashboard page composing ad type grid and platform suite section"
      contains: "AdTypeGrid"
    - path: "apps/web/src/features/dashboard/components/ad-type-card.tsx"
      provides: "Individual ad type browsing card"
      contains: "AdTypeCard"
    - path: "apps/web/src/features/dashboard/components/ad-type-grid.tsx"
      provides: "Responsive grid layout for ad type cards"
      contains: "AdTypeGrid"
    - path: "apps/web/src/features/dashboard/components/platform-suite-section.tsx"
      provides: "Platform Suite placeholder cards section"
      contains: "PLATFORM_SUITE"
    - path: "apps/web/src/components/layout/search-dialog.tsx"
      provides: "Command palette search across ad formats and platform sections"
      contains: "CommandDialog"
  key_links:
    - from: "apps/web/src/features/dashboard/pages/dashboard-page.tsx"
      to: "apps/web/src/features/dashboard/data/ad-types.ts"
      via: "imports AD_TYPES for card grid"
      pattern: "import.*AD_TYPES.*from.*ad-types"
    - from: "apps/web/src/features/dashboard/components/ad-type-card.tsx"
      to: "react-router Link"
      via: "Card wraps Link to /creatives?type={slug}"
      pattern: "Link.*to=.*creatives"
    - from: "apps/web/src/components/layout/search-dialog.tsx"
      to: "apps/web/src/features/dashboard/data/ad-types.ts"
      via: "imports AD_TYPES for searchable format list"
      pattern: "import.*AD_TYPES.*from.*ad-types"
    - from: "apps/web/src/components/layout/search-dialog.tsx"
      to: "react-router useNavigate"
      via: "onSelect navigates to format or section URL"
      pattern: "navigate\\("
    - from: "apps/web/src/router.tsx"
      to: "apps/web/src/features/dashboard/pages/dashboard-page.tsx"
      via: "lazy import for index route"
      pattern: "import.*dashboard-page"
---

<objective>
Build the dashboard home page with ad type browsing cards, Platform Suite placeholder section, and a global command palette search. Wire the dashboard into the router and connect the search dialog to the header trigger.

Purpose: This is the landing experience after login -- users see what ad types are available, can explore formats, and quickly navigate anywhere in the platform via search. The ad type cards establish the browsing paradigm that Phase 4 (Template Library) will build upon.

Output: Complete dashboard page with ad type cards, Platform Suite section, functional Ctrl+K search dialog, router updated with real dashboard page.
</objective>

<execution_context>
@C:/Users/HaseebRahman/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HaseebRahman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dashboard-navigation-shell/03-RESEARCH.md
@.planning/phases/03-dashboard-navigation-shell/03-01-SUMMARY.md

# Files from Plan 01 that this plan builds on
@apps/web/src/components/layout/app-shell.tsx
@apps/web/src/components/layout/app-header.tsx
@apps/web/src/router.tsx
@apps/web/src/components/ui/card.tsx
@apps/web/src/components/ui/command.tsx
@apps/web/src/components/ui/badge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ad type data model, dashboard components, and dashboard page</name>
  <files>
    apps/web/src/features/dashboard/data/ad-types.ts
    apps/web/src/features/dashboard/components/ad-type-card.tsx
    apps/web/src/features/dashboard/components/ad-type-grid.tsx
    apps/web/src/features/dashboard/components/platform-suite-section.tsx
    apps/web/src/features/dashboard/pages/dashboard-page.tsx
  </files>
  <action>
    **A. Create `features/dashboard/data/ad-types.ts`:**
    - Define TypeScript interfaces: `AdFormat` (id, name, slug, description), `AdType` (id, name, slug, description, icon: LucideIcon, formats: AdFormat[])
    - Define `AD_TYPES` constant array with 5 ad types and their formats (exact data from 03-RESEARCH.md Pattern 5):
      1. Interactive (MousePointerClick icon): Swipeable Carousel, 3D Cube Carousel, Scratch to Reveal, Flipcard, Quiz/Poll, Before/After Slider, Accordion (7 formats)
      2. Animated (Sparkles icon): Animated Banner, Countdown Timer (2 formats)
      3. Video (Film icon): Video with End Card, Click to Play (2 formats)
      4. Standard (Image icon): Static Banner, Multi-Frame Banner (2 formats)
      5. Native (Newspaper icon): In-Feed Native (1 format)
    - Define `PlatformSuiteItem` interface (id, name, description, icon: LucideIcon, comingSoon: true)
    - Define `PLATFORM_SUITE` constant array with 3 items:
      1. Audio (Volume2 icon) - "Audio ad creation and distribution"
      2. ADCTV (Tv icon) - "Connected TV ad builder"
      3. Social Display (Share2 icon) - "Social media display formats"
    - Export all interfaces and constants

    **B. Create `features/dashboard/components/ad-type-card.tsx`:**
    - Props: `adType: AdType`
    - Renders as a Link (react-router) wrapping a shadcn/ui Card
    - Link destination: `/creatives?type=${adType.slug}` (placeholder route until Phase 4)
    - Card content:
      - Icon in a colored background circle (bg-primary/10 with text-primary icon)
      - Type name as CardTitle
      - Badge showing format count (e.g., "7 formats")
      - CardDescription with the type description
      - List of up to 3 format names, with "+N more" if more than 3 formats exist
    - Hover effect: `transition-shadow hover:shadow-md` on the Card
    - Card fills full height in grid: `h-full`
    - Follow the exact component structure from 03-RESEARCH.md Code Example: Dashboard Ad Type Card Component

    **C. Create `features/dashboard/components/ad-type-grid.tsx`:**
    - Imports `AD_TYPES` from the data file and `AdTypeCard`
    - Renders a responsive CSS grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`
    - Maps over AD_TYPES to render an AdTypeCard for each
    - Include a heading above the grid: "Ad Types" with a subtitle like "Choose an ad type to browse templates and start creating"

    **D. Create `features/dashboard/components/platform-suite-section.tsx`:**
    - Imports `PLATFORM_SUITE` from the data file
    - Section with heading: "Platform Suite" with subtitle "Expand your reach with our upcoming products"
    - Renders a responsive grid of cards for each Platform Suite item
    - Each card: icon, name, description, and a "Coming Soon" Badge (variant="outline" or "secondary")
    - Cards should have a muted/dimmed appearance to visually distinguish from clickable ad type cards -- e.g., `opacity-75` or `bg-muted/50`
    - Cards are NOT clickable (no Link wrapper)
    - Separator above the section to visually separate from ad type cards

    **E. Create `features/dashboard/pages/dashboard-page.tsx`:**
    - Default export component
    - Composes: welcome heading, AdTypeGrid, PlatformSuiteSection
    - Welcome area at top: "Welcome to ScrollToday" heading, brief subtitle about the platform
    - Below welcome: AdTypeGrid (the main content)
    - Below grid: PlatformSuiteSection
    - Use appropriate spacing between sections (space-y-8 or space-y-10)
    - No standalone layout wrappers (AppShell provides the chrome)
  </action>
  <verify>
    - `cd apps/web && pnpm exec tsc --noEmit` passes
    - Files exist: ad-types.ts, ad-type-card.tsx, ad-type-grid.tsx, platform-suite-section.tsx, dashboard-page.tsx
    - ad-types.ts exports AD_TYPES (5 items), PLATFORM_SUITE (3 items), AdType, AdFormat, PlatformSuiteItem
    - ad-type-card.tsx imports from Card, Badge, Link
    - dashboard-page.tsx imports AdTypeGrid and PlatformSuiteSection
  </verify>
  <done>Dashboard page renders with 5 ad type cards in a responsive grid (each showing icon, name, format count, description, format preview list), 3 Platform Suite placeholder cards with "Coming Soon" treatment, and a welcome heading</done>
</task>

<task type="auto">
  <name>Task 2: Create search dialog, wire to header, update router with dashboard page</name>
  <files>
    apps/web/src/components/layout/search-dialog.tsx
    apps/web/src/components/layout/app-shell.tsx
    apps/web/src/components/layout/app-header.tsx
    apps/web/src/router.tsx
  </files>
  <action>
    **A. Create `components/layout/search-dialog.tsx`:**
    - Uses shadcn/ui CommandDialog, CommandInput, CommandList, CommandGroup, CommandItem, CommandEmpty
    - State: `open` boolean, controlled by parent via props OR internal keyboard listener
    - Keyboard shortcut: `useEffect` that listens for Ctrl+K (or Cmd+K on Mac) to toggle open state
    - Two search groups:
      1. "Ad Formats" -- flatMap over AD_TYPES to list all formats with their parent type name: `{format.name} ({type.name})`
         - On select: navigate to `/creatives?type=${type.slug}&format=${format.slug}`
      2. "Platform Sections" -- Dashboard, Creatives, Campaigns, Analytics, Billing, Settings
         - On select: navigate to the section URL
    - Use `useNavigate` from react-router for navigation
    - Close dialog after selection
    - Import AD_TYPES from the dashboard data module
    - Add icons to search items: use the AdType icon for format results, use section-specific icons for platform sections (matching sidebar icons)
    - Follow the exact pattern from 03-RESEARCH.md Pattern 4: Command Palette Search
    - Export the component AND a way for the header to trigger it. Options:
      - Option A (preferred): SearchDialog manages its own open state internally via keyboard shortcut, and also accepts an `open` prop and `onOpenChange` prop for external control
      - Option B: Use a simple React context or callback ref

    **B. Update `components/layout/app-shell.tsx`:**
    - Import and render `SearchDialog` inside the SidebarProvider (after SidebarInset, as a sibling)
    - Pass props if needed for header integration
    - The SearchDialog should be rendered as a portal-based dialog, so its exact position in the tree doesn't affect layout

    **C. Update `components/layout/app-header.tsx`:**
    - The search trigger (the visual input-like element showing "Search... Ctrl+K") should now actually open the search dialog when clicked
    - Implementation approach: The header's search trigger should call a function to open the search dialog. Since SearchDialog listens for Ctrl+K globally, clicking the trigger can programmatically dispatch a Ctrl+K keydown event, OR the SearchDialog can expose its state via a shared mechanism.
    - Simplest approach: Have the header search trigger simply programmatically dispatch a keyboard event (`new KeyboardEvent('keydown', { key: 'k', ctrlKey: true })` on `document`) which the SearchDialog's listener will pick up. This avoids prop drilling.
    - Alternatively, use a simple callback passed through AppShell.

    **D. Update `router.tsx`:**
    - Replace the inline dashboard placeholder (from Plan 01) with a lazy-loaded import of the real DashboardPage:
      ```tsx
      {
        index: true,
        lazy: async () => {
          const { default: Component } = await import('@/features/dashboard/pages/dashboard-page')
          return { Component }
        },
      }
      ```
    - Keep all other routes unchanged
    - Remove the temporary dashboard placeholder if it still exists
  </action>
  <verify>
    - `cd apps/web && pnpm exec tsc --noEmit` passes
    - `cd apps/web && pnpm build` succeeds
    - search-dialog.tsx imports CommandDialog and AD_TYPES
    - app-shell.tsx renders SearchDialog
    - router.tsx has lazy import for dashboard-page (not inline placeholder)
    - Pressing Ctrl+K on any page opens the search dialog
    - Typing in the search dialog filters ad formats and platform sections
    - Selecting a result navigates to the correct URL and closes the dialog
  </verify>
  <done>Search dialog opens via Ctrl+K or clicking header search trigger, shows all 14 ad formats grouped by type and 6 platform sections, selecting a result navigates correctly. Dashboard page lazy-loads from features/dashboard/pages/dashboard-page.tsx. Full build succeeds.</done>
</task>

</tasks>

<verification>
1. Navigate to dashboard (`/`) after login -- see welcome heading, 5 ad type cards in responsive grid, Platform Suite section below
2. Ad type cards show: icon, name, format count badge, description, first 3 format names (Interactive card shows "+4 more")
3. Click an ad type card -- navigates to `/creatives?type={slug}` (shows placeholder page)
4. Platform Suite shows 3 cards (Audio, ADCTV, Social Display) with "Coming Soon" badges, visually muted, not clickable
5. Press Ctrl+K -- search dialog opens with 14 ad formats and 6 platform sections
6. Type "carousel" in search -- filters to show Swipeable Carousel and 3D Cube Carousel
7. Select "Swipeable Carousel" -- navigates to `/creatives?type=interactive&format=carousel`, dialog closes
8. Type "settings" -- shows Settings in Platform Sections, select it -- navigates to `/settings`
9. Click the search trigger in the header -- opens the same search dialog
10. Dashboard is responsive: 1 column on mobile, 2 on tablet, 3 on desktop
</verification>

<success_criteria>
- Dashboard shows all 5 ad types with correct format counts and descriptions
- Platform Suite shows all 3 placeholder products with "Coming Soon" treatment
- Search dialog finds all 14 formats and 6 platform sections
- Search result selection navigates correctly
- TypeScript compiles, production build succeeds
- All components follow established patterns (shadcn/ui components, react-router, feature folder structure)
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-navigation-shell/03-02-SUMMARY.md`
</output>
