---
phase: 03-dashboard-navigation-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/index.css
  - apps/web/src/components/ui/*.tsx
  - apps/web/src/components/layout/app-shell.tsx
  - apps/web/src/components/layout/app-sidebar.tsx
  - apps/web/src/components/layout/app-header.tsx
  - apps/web/src/components/layout/app-footer.tsx
  - apps/web/src/router.tsx
  - apps/web/src/features/auth/pages/settings-page.tsx
  - apps/web/src/features/admin/pages/admin-users-page.tsx
autonomous: true

must_haves:
  truths:
    - "Sidebar navigation is visible on all authenticated pages with links to Dashboard, Creatives, Campaigns, Analytics, Billing, Settings, and Admin > Users (for super_admin)"
    - "Sidebar collapses to icon mode and expands back; Cmd/Ctrl+B toggles it"
    - "Footer with placeholder links (Help & Support, Showcase, Creative Policy, Privacy Policy, Terms of Service) appears at the bottom of every page"
    - "Settings and Admin Users pages render correctly inside the shared shell without double backgrounds or double padding"
    - "Unauthenticated routes (login, change-password) do NOT show the sidebar/shell"
  artifacts:
    - path: "apps/web/src/components/layout/app-shell.tsx"
      provides: "Root layout component: SidebarProvider + AppSidebar + SidebarInset + AppHeader + Outlet + AppFooter"
      contains: "SidebarProvider"
    - path: "apps/web/src/components/layout/app-sidebar.tsx"
      provides: "Sidebar with navigation items, user section, admin group"
      contains: "SidebarMenuButton"
    - path: "apps/web/src/components/layout/app-header.tsx"
      provides: "Top header with sidebar trigger, breadcrumb, search input placeholder"
      contains: "SidebarTrigger"
    - path: "apps/web/src/components/layout/app-footer.tsx"
      provides: "Footer with placeholder links"
      contains: "footerLinks"
    - path: "apps/web/src/router.tsx"
      provides: "Updated route tree with AppShell layout route wrapping authenticated children"
      contains: "AppShell"
  key_links:
    - from: "apps/web/src/router.tsx"
      to: "apps/web/src/components/layout/app-shell.tsx"
      via: "layout route element"
      pattern: "element:\\s*<AppShell"
    - from: "apps/web/src/components/layout/app-sidebar.tsx"
      to: "react-router Link"
      via: "SidebarMenuButton asChild with Link"
      pattern: "asChild.*\\n.*<Link"
    - from: "apps/web/src/components/layout/app-sidebar.tsx"
      to: "apps/web/src/contexts/auth-context.tsx"
      via: "useAuth for user info and isAdmin check"
      pattern: "useAuth"
---

<objective>
Install all required shadcn/ui components and build the application shell: collapsible sidebar navigation, top header bar, and footer with placeholder links. Refactor the router to use a layout route pattern and update existing pages to remove their standalone layout wrappers.

Purpose: Establishes the persistent navigation chrome for all authenticated pages -- the sidebar, header, and footer that will be present throughout the rest of the application. This is the structural foundation that all subsequent phases' pages will render inside.

Output: AppShell layout wrapping all authenticated routes, collapsible sidebar with nav items, header with breadcrumb/search trigger area, footer with placeholder links, refactored existing pages.
</objective>

<execution_context>
@C:/Users/HaseebRahman/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HaseebRahman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dashboard-navigation-shell/03-RESEARCH.md

# Key existing files to understand before modifying
@apps/web/src/router.tsx
@apps/web/src/contexts/auth-context.tsx
@apps/web/src/features/auth/pages/settings-page.tsx
@apps/web/src/features/admin/pages/admin-users-page.tsx
@apps/web/src/features/auth/components/protected-route.tsx
@apps/web/components.json
@apps/web/src/index.css
@apps/web/src/lib/utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn/ui components and configure CSS theme variables</name>
  <files>
    apps/web/src/components/ui/*.tsx
    apps/web/src/index.css
    apps/web/package.json
  </files>
  <action>
    From the `apps/web` directory, install runtime dependencies and shadcn/ui components:

    1. Install runtime dependencies first:
       ```bash
       cd apps/web && pnpm add lucide-react cmdk tw-animate-css class-variance-authority
       ```

    2. Install shadcn/ui components via CLI. The CLI reads `components.json` (already configured with new-york style, neutral base, CSS variables, @/components/ui alias). Run:
       ```bash
       cd apps/web && pnpx shadcn@latest add sidebar command card button avatar dropdown-menu tooltip separator badge input skeleton sheet breadcrumb collapsible
       ```
       If the CLI prompts interactively, use `--yes` or `--overwrite` flags. The CLI will:
       - Copy component source files into `src/components/ui/`
       - Install required Radix UI peer dependencies automatically
       - May update `index.css` with CSS variable definitions for theming

    3. After the CLI runs, verify `src/components/ui/sidebar.tsx` exists. If it does not (some shadcn versions), manually install it using the CLI or copy from shadcn/ui documentation.

    4. Check `src/index.css` after the CLI runs. If the CLI added CSS variable definitions (`:root { --background: ...; --foreground: ...; }` etc.), leave them as-is -- they provide the neutral theme. If the CLI did NOT add them, manually add the neutral theme CSS variables from shadcn/ui's theming system. The file should contain:
       - `@import "tailwindcss"`
       - `@import "tw-animate-css"` (for animation utilities)
       - `:root` and `.dark` CSS variable blocks with the neutral color palette
       - `@theme inline` block mapping CSS variables to Tailwind tokens (if required by Tailwind CSS 4)
       - Sidebar-specific CSS variables: `--sidebar-background`, `--sidebar-foreground`, `--sidebar-primary`, `--sidebar-accent`, etc.

    5. Verify the build still compiles:
       ```bash
       cd apps/web && pnpm exec tsc --noEmit
       ```

    IMPORTANT: The project uses Tailwind CSS 4 (CSS-first config, `@import "tailwindcss"`, no tailwind.config.js). Ensure any CSS variable setup follows Tailwind CSS 4 conventions. The shadcn/ui CLI should handle this correctly given the `components.json` configuration.
  </action>
  <verify>
    - `ls apps/web/src/components/ui/` shows sidebar.tsx, command.tsx, card.tsx, button.tsx, avatar.tsx, dropdown-menu.tsx, tooltip.tsx, separator.tsx, badge.tsx, input.tsx, skeleton.tsx, sheet.tsx, breadcrumb.tsx, collapsible.tsx
    - `apps/web/src/index.css` contains CSS variable definitions (`:root` block with `--background`, `--sidebar-background`, etc.)
    - `cd apps/web && pnpm exec tsc --noEmit` passes without errors
    - `cd apps/web && pnpm build` completes successfully
  </verify>
  <done>All shadcn/ui components installed in src/components/ui/, CSS theme variables configured, TypeScript compiles, build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Create AppShell layout, sidebar, header, footer, refactor router and existing pages</name>
  <files>
    apps/web/src/components/layout/app-shell.tsx
    apps/web/src/components/layout/app-sidebar.tsx
    apps/web/src/components/layout/app-header.tsx
    apps/web/src/components/layout/app-footer.tsx
    apps/web/src/router.tsx
    apps/web/src/features/auth/pages/settings-page.tsx
    apps/web/src/features/admin/pages/admin-users-page.tsx
  </files>
  <action>
    **A. Create `components/layout/app-footer.tsx`:**
    - Static footer component with placeholder links
    - Links array: Help & Support, Showcase, Creative Policy, Privacy Policy, Terms of Service -- all `href="#"`
    - Uses Separator from shadcn/ui above the footer content
    - Copyright line: `© {new Date().getFullYear()} ScrollToday. All rights reserved.`
    - Responsive layout: flex-wrap with justify-between on md+, stacked on mobile
    - `mt-auto` class so footer sticks to bottom of content area
    - Follow the exact pattern from 03-RESEARCH.md Pattern: Footer with Placeholder Links

    **B. Create `components/layout/app-header.tsx`:**
    - Top header bar inside SidebarInset (above main content)
    - Contains: SidebarTrigger (hamburger/collapse button), Separator (vertical), Breadcrumb showing current section
    - Search trigger area: a clickable div/button styled like a search input showing "Search... (Ctrl+K)" that will later open the search dialog (search dialog is built in Plan 02). For now, just render the visual trigger without functionality -- it won't open anything yet.
    - Use `useSidebar` hook if needed for responsive behavior
    - Use `Breadcrumb`, `BreadcrumbItem`, `BreadcrumbLink`, `BreadcrumbList`, `BreadcrumbPage`, `BreadcrumbSeparator` from shadcn/ui
    - Derive breadcrumb from `useLocation().pathname`:
      - `/` -> "Dashboard"
      - `/settings` -> "Settings"
      - `/admin/users` -> "Admin" > "Users"
      - `/creatives` -> "Creatives"
      - etc.
    - Wrap header content in `<header className="flex h-16 shrink-0 items-center gap-2 border-b px-4">`

    **C. Create `components/layout/app-sidebar.tsx`:**
    - Uses shadcn/ui Sidebar with `collapsible="icon"` for collapse-to-icons mode
    - SidebarHeader: ScrollToday logo/brand text. When collapsed, show just "ST" or a small icon.
    - SidebarContent with two SidebarGroups:
      1. **Platform** group: Dashboard (`/`, LayoutDashboard icon), Creatives (`/creatives`, Palette), Campaigns (`/campaigns`, Megaphone), Analytics (`/analytics`, BarChart3), Billing (`/billing`, CreditCard)
      2. **Account** group: Settings (`/settings`, Settings icon)
    - Conditionally show **Admin** group (only if `isAdmin` from `useAuth()`): Users (`/admin/users`, Users icon)
    - Active state: `isActive={location.pathname === item.url}` using `useLocation()`
    - For sections without pages yet (Creatives, Campaigns, Analytics, Billing): link to their routes. These routes will render a simple placeholder component (defined in router.tsx as an inline component showing "[Section Name] - Coming in Phase X").
    - SidebarFooter: User section with Avatar (showing user initials from email), user email, DropdownMenu with options: Settings (Link to /settings), Sign Out (calls signOut from useAuth). Use `useAuth()` for user email, profile, signOut.
    - SidebarRail at the end for the resize/collapse rail
    - All navigation items use `SidebarMenuButton asChild` with react-router `<Link to={...}>` and `tooltip={item.title}` for collapsed mode labels
    - Import icons from lucide-react: LayoutDashboard, Palette, Megaphone, BarChart3, CreditCard, Settings, Users, LogOut, ChevronsUpDown

    **D. Create `components/layout/app-shell.tsx`:**
    - Root layout component that composes everything:
      ```tsx
      <TooltipProvider>
        <SidebarProvider>
          <AppSidebar />
          <SidebarInset>
            <AppHeader />
            <main className="flex-1 overflow-auto p-4 md:p-6">
              <Outlet />
            </main>
            <AppFooter />
          </SidebarInset>
        </SidebarProvider>
      </TooltipProvider>
      ```
    - TooltipProvider wraps everything (required by Sidebar icon-mode tooltips)
    - Outlet from react-router renders child page content

    **E. Refactor `router.tsx`:**
    - Remove the `DashboardPlaceholder` component entirely
    - Remove the `useAuth` and `Link` imports (no longer needed in router)
    - Import `AppShell` from `@/components/layout/app-shell`
    - Restructure route tree:
      ```
      /login -> LoginPage (no shell)
      ProtectedRoute ->
        /change-password -> ChangePasswordPage (no shell, full-screen)
        AppShell ->
          / (index) -> DashboardPage (lazy, from features/dashboard/pages/dashboard-page -- Plan 02 creates this, so for now use a simple inline placeholder: `function DashboardTemp() { return <div>Dashboard loading...</div> }`)
          /settings -> SettingsPage (lazy)
          /creatives -> placeholder page
          /campaigns -> placeholder page
          /analytics -> placeholder page
          /billing -> placeholder page
          AdminRoute ->
            /admin/users -> AdminUsersPage (lazy)
      ```
    - For placeholder pages (creatives, campaigns, analytics, billing), create a simple inline `SectionPlaceholder` component that reads the section name from the route and shows "Coming in Phase X" text. Use a small utility:
      ```tsx
      function SectionPlaceholder({ title, phase }: { title: string; phase: number }) {
        return (
          <div className="flex flex-col items-center justify-center py-20">
            <h2 className="text-2xl font-bold text-gray-900">{title}</h2>
            <p className="mt-2 text-muted-foreground">Coming in Phase {phase}</p>
          </div>
        )
      }
      ```
    - The `/` index route should use an inline placeholder for now since the actual DashboardPage is created in Plan 02. Use the same SectionPlaceholder with title "Dashboard" and phase 3.

    **F. Refactor `settings-page.tsx`:**
    - Remove the outer `<div className="min-h-screen bg-gray-50">` wrapper
    - The page should start directly with `<div className="max-w-2xl mx-auto ...">` (remove px-4 sm:px-6 lg:px-8 and py-8 since AppShell main provides padding)
    - Keep all internal content (Account Info section, Change Password section) intact
    - Only strip the outermost full-page wrapper

    **G. Refactor `admin-users-page.tsx`:**
    - Remove the outer `<div className="min-h-screen bg-gray-50">` wrapper
    - The page should start directly with the content container (remove redundant padding that AppShell provides)
    - Keep all internal content (header with Create User button, user list, dialogs) intact
    - Only strip the outermost full-page wrapper
  </action>
  <verify>
    - `cd apps/web && pnpm exec tsc --noEmit` passes
    - `cd apps/web && pnpm build` succeeds
    - Files exist: app-shell.tsx, app-sidebar.tsx, app-header.tsx, app-footer.tsx
    - router.tsx imports AppShell and uses it as layout route element
    - router.tsx does NOT contain DashboardPlaceholder
    - settings-page.tsx does NOT contain "min-h-screen"
    - admin-users-page.tsx does NOT contain "min-h-screen"
    - `pnpm dev` shows sidebar on left, header on top, footer at bottom on the dashboard route
  </verify>
  <done>
    AppShell layout renders with collapsible sidebar (6 main nav items + admin section), header with breadcrumb and search trigger placeholder, footer with 5 placeholder links. Router uses layout route pattern. Settings and Admin pages render inside the shell without double backgrounds. Placeholder pages exist for Creatives, Campaigns, Analytics, Billing. Login and change-password remain full-screen without shell.
  </done>
</task>

</tasks>

<verification>
1. Navigate to `/login` -- should see login page WITHOUT sidebar/header/footer
2. Log in -- should redirect to `/` showing dashboard placeholder inside the shell (sidebar + header + footer visible)
3. Click each sidebar nav item -- should navigate to correct route, active state highlights current item
4. Click sidebar collapse rail or press Cmd/Ctrl+B -- sidebar collapses to icon mode, expands back
5. Hover collapsed sidebar icons -- tooltip shows item name
6. Footer visible at bottom of every page with 5 placeholder links
7. Navigate to `/settings` -- page renders correctly inside shell (no double background)
8. Navigate to `/admin/users` (as super_admin) -- page renders correctly inside shell (no double background)
9. Admin nav group only visible when logged in as super_admin
10. `/change-password` renders full-screen without shell (forced password change flow)
</verification>

<success_criteria>
- All authenticated pages share the sidebar + header + footer shell
- Sidebar has all required navigation items with correct icons and active states
- Footer shows all 5 placeholder links
- Existing pages (Settings, Admin Users) render correctly without layout conflicts
- TypeScript compiles, production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-navigation-shell/03-01-SUMMARY.md`
</output>
