---
phase: 08-ad-serving-infrastructure
plan: 04
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All three Edge Functions (serve-ad, track-event, click-redirect) are deployed to Supabase"
    - "Ad tag URL returns JavaScript that renders a creative in an iframe"
    - "Impression event appears in ad_events table after ad loads"
    - "Click event appears in ad_events table and browser redirects to destination"
  artifacts: []
  key_links:
    - from: "ad tag (script src)"
      to: "supabase/functions/serve-ad"
      via: "HTTP GET returning application/javascript"
      pattern: "serve-ad"
    - from: "impression pixel (img src)"
      to: "supabase/functions/track-event"
      via: "HTTP GET returning 1x1 GIF"
      pattern: "track-event"
    - from: "CTA click (href)"
      to: "supabase/functions/click-redirect"
      via: "HTTP GET returning 302 redirect"
      pattern: "click-redirect"

user_setup:
  - service: supabase
    why: "Edge Functions deployment requires database URL for migration push"
    env_vars:
      - name: DATABASE_URL
        source: "Supabase Dashboard -> Settings -> Database -> Connection string (URI)"
---

<objective>
Deploy all three ad serving Edge Functions to Supabase and verify the complete ad serving flow end-to-end.

Purpose: Edge Functions must be deployed with `--no-verify-jwt` since they receive unauthenticated requests from ad tags on publisher pages. The database migration from Plan 01 must also be pushed. End-to-end verification confirms the full serving -> tracking -> redirect chain works.

Output: Deployed Edge Functions accessible via Supabase project URL, verified ad serving flow.
</objective>

<execution_context>
@C:/Users/HaseebRahman/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HaseebRahman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ad-serving-infrastructure/08-RESEARCH.md
@.planning/phases/08-ad-serving-infrastructure/08-01-SUMMARY.md
@.planning/phases/08-ad-serving-infrastructure/08-02-SUMMARY.md
@.planning/phases/08-ad-serving-infrastructure/08-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy database migration and Edge Functions</name>
  <files></files>
  <action>
Deploy all Phase 8 artifacts to Supabase:

1. **Push database migration** (credit_balance, rendered_html, deduct_impression_credit):
   ```bash
   npx supabase db push --db-url "$DATABASE_URL"
   ```
   This applies `20260224000001_credit_balance_serving.sql`. Uses the same `--db-url` pattern established in Phase 1 (no Docker dependency, no CLI access token needed).

2. **Deploy serve-ad Edge Function** with --no-verify-jwt (receives unauthenticated ad tag requests):
   ```bash
   npx supabase functions deploy serve-ad --no-verify-jwt
   ```

3. **Deploy track-event Edge Function** with --no-verify-jwt (receives unauthenticated pixel requests):
   ```bash
   npx supabase functions deploy track-event --no-verify-jwt
   ```

4. **Deploy click-redirect Edge Function** with --no-verify-jwt (receives unauthenticated click requests):
   ```bash
   npx supabase functions deploy click-redirect --no-verify-jwt
   ```

5. **Verify deployments** by checking function URLs respond:
   - `curl -s -o /dev/null -w "%{http_code}" "https://PROJECT_REF.supabase.co/functions/v1/serve-ad"` should return 204 (no creative ID = empty response)
   - `curl -s -o /dev/null -w "%{http_code}" "https://PROJECT_REF.supabase.co/functions/v1/track-event"` should return 200 with GIF content-type (missing params = still returns pixel)
   - `curl -s -o /dev/null -w "%{http_code}" "https://PROJECT_REF.supabase.co/functions/v1/click-redirect"` should return 400 (missing required dest param)

If `supabase functions deploy` requires authentication (access token), this will be handled dynamically -- the user will be prompted to run `npx supabase login` if needed. The existing admin functions (admin-create-user, admin-list-users, admin-reset-password) were deployed previously, so the CLI should already be configured.

Note: The `--db-url` flag on `db push` connects directly to PostgreSQL, bypassing the need for `supabase link`. Read the DATABASE_URL from .env.development or prompt user if not available.
  </action>
  <verify>
- Migration applies without errors (credit_balance column, rendered_html column, deduct function all created)
- All 3 Edge Functions deployed (supabase functions list shows serve-ad, track-event, click-redirect)
- serve-ad returns 204 for requests without creative ID
- track-event returns 200 with image/gif content-type
- click-redirect returns 400 for requests without dest parameter
  </verify>
  <done>Database migration pushed and all 3 Edge Functions deployed with --no-verify-jwt. Each responds correctly to basic requests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify end-to-end ad serving flow</name>
  <files></files>
  <action>
Human verification checkpoint. Claude has deployed all ad serving infrastructure. User verifies the complete flow works end-to-end.

What was built:
- serve-ad Edge Function: returns application/javascript that creates iframe with creative HTML and embedded tracking
- track-event Edge Function: receives 1x1 pixel/beacon requests and logs to ad_events
- click-redirect Edge Function: logs click events and 302 redirects to destination URL
- Database: credit_balance column, rendered_html column, atomic deduct_impression_credit function

Steps for verification:

**Pre-requisite:** You need at least one creative with `status = 'active'` and `rendered_html` populated. To populate rendered_html for existing creatives:
1. Open any existing creative in the editor (e.g., navigate to /creatives/{id}/edit)
2. Click "Save" (this now generates and stores rendered_html)
3. Set the creative status to "Active" via the campaign detail page

**Option A: Quick functional test (recommended)**
1. Open browser DevTools Network tab
2. Navigate to: `https://PROJECT_REF.supabase.co/functions/v1/serve-ad?id=CREATIVE_ID&w=300&h=250&cb=12345`
3. Expected: JavaScript response with `application/javascript` content-type
4. The JavaScript should contain `srcdoc` with the creative HTML

**Option B: Full tag test**
1. Create a simple test HTML file locally:
   ```html
   <!DOCTYPE html>
   <html><body>
   <h1>Ad Tag Test</h1>
   <div style="width:300px;height:250px;border:1px solid #ccc;">
     <script src="https://PROJECT_REF.supabase.co/functions/v1/serve-ad?id=CREATIVE_ID&w=300&h=250&cb=12345"></script>
   </div>
   </body></html>
   ```
2. Open in browser (must be served over HTTPS for cookies to work, or use localhost)
3. Expected: The ad creative renders inside the div
4. Check Network tab: impression pixel request to track-event should fire
5. Click the ad CTA: should go through click-redirect endpoint before reaching destination URL
6. Check ad_events table in Supabase SQL Editor: `SELECT * FROM ad_events ORDER BY event_timestamp DESC LIMIT 10;`
7. Expected: impression_served event with the creative's request_id

**Verify credit deduction:**
1. In Supabase SQL Editor: `UPDATE advertisers SET credit_balance = 5 WHERE id = 'ADVERTISER_ID';`
2. Load the ad tag 6 times
3. Check: `SELECT credit_balance FROM advertisers WHERE id = 'ADVERTISER_ID';`
4. Expected: credit_balance = 0 (not negative)
5. Load the ad tag again -- should get empty response (no ad)
  </action>
  <verify>Type "approved" if the ad serves correctly with tracking, or describe any issues.</verify>
  <done>User has verified: ad tag renders creative, impression events appear in ad_events, clicks redirect correctly, credit deduction is atomic and stops at zero.</done>
</task>

</tasks>

<verification>
- All 3 Edge Functions deployed and accessible
- Ad tag renders creative in iframe
- Impression event logged in ad_events
- Click redirects through click-redirect endpoint
- Credit balance decrements atomically (never goes negative)
- Zero balance results in no ad served (204 response)
</verification>

<success_criteria>
All 5 Phase 8 success criteria verified:
1. Loading an ad tag renders the correct creative with assets from CDN
2. Impression tracking pixel fires and event appears in ad_events with request_id
3. Click tracking works with redirect to destination URL
4. Viewability tracked via IntersectionObserver (50% visible for 1s)
5. Zero credit balance stops serving (204 response, no negative balances)
</success_criteria>

<output>
After completion, create `.planning/phases/08-ad-serving-infrastructure/08-04-SUMMARY.md`
</output>
