---
phase: 09-billing-credit-system
plan: 03
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - apps/web/src/features/billing/components/credit-pack-card.tsx
  - apps/web/src/features/billing/components/transaction-table.tsx
  - apps/web/src/features/billing/pages/billing-page.tsx
  - apps/web/src/router.tsx
autonomous: false
requirements: [BILL-01, BILL-03, BILL-05]

must_haves:
  truths:
    - "User can see three credit pack options (50k, 200k, 1M) on the billing page"
    - "Clicking Buy on a credit pack redirects to Stripe hosted Checkout page"
    - "After successful purchase, user returns to /billing?success=true and sees a success message"
    - "Transaction history table shows all credit purchases with date, amount, type, and receipt link"
    - "User can create and preview ads without purchasing credits (no credit checks in editor flow)"
  artifacts:
    - path: "apps/web/src/features/billing/components/credit-pack-card.tsx"
      provides: "Credit pack selection card with price, impression count, and Buy button"
      exports: ["CreditPackCard"]
    - path: "apps/web/src/features/billing/components/transaction-table.tsx"
      provides: "Transaction history table with date, type, amount, balance_after columns"
      exports: ["TransactionTable"]
    - path: "apps/web/src/features/billing/pages/billing-page.tsx"
      provides: "Main billing page with credit packs, balance summary, and transaction history"
      exports: ["default"]
    - path: "apps/web/src/router.tsx"
      provides: "Router with /billing pointing to billing-page (replaces placeholder)"
      contains: "billing-page"
  key_links:
    - from: "apps/web/src/features/billing/pages/billing-page.tsx"
      to: "apps/web/src/features/billing/hooks/use-billing.ts"
      via: "useTransactions and useCreateCheckout hooks"
      pattern: "useTransactions|useCreateCheckout"
    - from: "apps/web/src/features/billing/pages/billing-page.tsx"
      to: "apps/web/src/features/billing/hooks/use-credit-balance.ts"
      via: "useCreditBalance for balance summary"
      pattern: "useCreditBalance"
    - from: "apps/web/src/features/billing/pages/billing-page.tsx"
      to: "apps/web/src/features/billing/components/credit-pack-card.tsx"
      via: "renders pack cards"
      pattern: "CreditPackCard"
    - from: "apps/web/src/features/billing/pages/billing-page.tsx"
      to: "apps/web/src/features/billing/components/transaction-table.tsx"
      via: "renders transaction history"
      pattern: "TransactionTable"
    - from: "apps/web/src/router.tsx"
      to: "apps/web/src/features/billing/pages/billing-page.tsx"
      via: "lazy import for /billing route"
      pattern: "billing-page"
---

<objective>
Billing page with credit pack purchase cards, Stripe Checkout redirect, purchase success handling, and transaction history table. Route update to replace the Phase 9 placeholder.

Purpose: Gives advertisers the full billing experience -- browse credit packs, purchase via Stripe, see purchase confirmation, and review transaction history with receipts (BILL-01, BILL-03). Includes verification that creating/previewing ads requires no credits (BILL-05).

Output: Complete billing page, credit pack cards, transaction table, router update.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-billing-credit-system/09-RESEARCH.md
@.planning/phases/09-billing-credit-system/09-01-SUMMARY.md
@.planning/phases/09-billing-credit-system/09-02-SUMMARY.md

@apps/web/src/router.tsx
@apps/web/src/features/billing/api/billing-api.ts
@apps/web/src/features/billing/hooks/use-billing.ts
@apps/web/src/features/billing/hooks/use-credit-balance.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Credit pack cards, transaction table, and billing page</name>
  <files>apps/web/src/features/billing/components/credit-pack-card.tsx, apps/web/src/features/billing/components/transaction-table.tsx, apps/web/src/features/billing/pages/billing-page.tsx, apps/web/src/router.tsx</files>
  <action>
Create `apps/web/src/features/billing/components/credit-pack-card.tsx`:
- `CreditPackCard` component accepting props: `{ packId: string; credits: number; label: string; price: string; popular?: boolean; onPurchase: (packId: string) => void; loading?: boolean }`
- Uses shadcn/ui Card component (`Card`, `CardHeader`, `CardTitle`, `CardDescription`, `CardContent`, `CardFooter`)
- Layout:
  - CardHeader: label (e.g., "50,000 Impressions"), price below
  - CardContent: brief description or value proposition
  - CardFooter: "Buy Now" Button (shadcn/ui Button)
  - If `popular`: add a "Most Popular" badge (shadcn/ui Badge) at the top
- Button calls `onPurchase(packId)` on click
- Button shows loading spinner when `loading` is true (use `disabled` + Loader2 icon from lucide-react)
- Tailwind: card hover effect with `transition-shadow hover:shadow-lg`, border highlight on popular pack

Create `apps/web/src/features/billing/components/transaction-table.tsx`:
- `TransactionTable` component accepting props: `{ transactions: CreditTransaction[]; loading?: boolean }`
- Define `CreditTransaction` type inline (or import from database.types.ts Tables<'credit_transactions'>):
  - id, type, amount, balance_after, stripe_session_id, receipt_url, metadata (JSONB), created_at
- Uses shadcn/ui Table components (`Table`, `TableBody`, `TableCell`, `TableHead`, `TableHeader`, `TableRow`)
- Columns:
  - Date: formatted with `new Date(tx.created_at).toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' })`
  - Type: Badge with color coding (purchase = green, adjustment = blue, refund = red)
  - Credits: formatted with `.toLocaleString()` and + prefix for positive amounts
  - Balance After: formatted with `.toLocaleString()`
  - Receipt: if `receipt_url` exists, external link icon that opens Stripe receipt in new tab; otherwise "---"
- Loading state: show skeleton rows (3-4 rows with pulsing placeholders)
- Empty state: "No transactions yet. Purchase credits to get started." with link to credit packs section

Create `apps/web/src/features/billing/pages/billing-page.tsx`:
- Default export `BillingPage` component
- Uses hooks from Plan 02: `useCreditBalance()`, `useTransactions()`, `useCreateCheckout()`
- Parse URL search params for `?success=true` and `?canceled=true` (from Stripe redirect)
- Layout (top to bottom):
  1. **Page header**: "Billing" title with description "Manage your impression credits and view purchase history"
  2. **Success banner** (conditionally shown when `?success=true` in URL):
     - Green alert/banner: "Payment successful! Your credits are being added to your account."
     - Use shadcn/ui Alert with `variant="default"` and green styling
     - Show a polling indicator: on success redirect, set a shorter refetch interval (3 seconds for 30 seconds) to catch the webhook fulfillment quickly. Use `useQueryClient().invalidateQueries({ queryKey: ['credit-balance'] })` on mount when success=true.
     - After 5 seconds, remove the success param from URL via `window.history.replaceState`
  3. **Canceled banner** (conditionally shown when `?canceled=true`):
     - Yellow alert: "Checkout was canceled. No charges were made."
  4. **Balance summary card**:
     - Large balance number (formatted), label "Available Impression Credits"
     - Using shadcn/ui Card with large typography
  5. **Credit packs section**:
     - Section title: "Purchase Credits"
     - Description: "Select a credit pack to purchase. Credits never expire."
     - Three `CreditPackCard` components in a responsive grid (`grid grid-cols-1 md:grid-cols-3 gap-6`):
       - 50k pack: "Starter" -- "50,000 Impressions" -- placeholder price "$49" -- packId "50k"
       - 200k pack: "Growth" -- "200,000 Impressions" -- placeholder price "$149" -- popular=true -- packId "200k"
       - 1M pack: "Scale" -- "1,000,000 Impressions" -- placeholder price "$499" -- packId "1m"
     - Each card's onPurchase calls `createCheckout.mutate(packId)` which redirects to Stripe
     - Pass `loading={createCheckout.isPending}` to disable all cards during checkout creation
  6. **Transaction history section**:
     - Section title: "Transaction History"
     - `TransactionTable` component with `transactions` from `useTransactions()` hook
     - Show loading skeleton while transactions are fetching
  7. **Free tier note** (bottom of page):
     - Info card or note: "Creating and previewing ads is always free. Credits are only consumed when your ads are served to real users."
     - This satisfies BILL-05 verification with visible messaging

Update `apps/web/src/router.tsx`:
- Replace the `/billing` placeholder route:
  ```
  // FROM:
  { path: '/billing', element: <SectionPlaceholder title="Billing" phase={9} /> }
  // TO:
  { path: '/billing', lazy: async () => { const { default: Component } = await import('@/features/billing/pages/billing-page'); return { Component } } }
  ```
- This follows the exact lazy loading pattern used by all other routes (creatives, campaigns, etc.)
  </action>
  <verify>
- All component files exist with correct exports
- BillingPage uses useCreditBalance, useTransactions, useCreateCheckout hooks
- CreditPackCard renders 3 packs in grid layout
- TransactionTable shows formatted transaction data
- Router lazy-loads billing-page (no more placeholder)
- Success/canceled URL params handled
- `pnpm run build` compiles without new errors
- Start dev server, navigate to /billing, confirm page renders with credit packs and transaction table (empty initially)
  </verify>
  <done>
Billing page shows balance summary, 3 credit pack cards (50k/200k/1M), Buy Now buttons that invoke create-checkout Edge Function, success/cancel handling for Stripe redirect, transaction history table with receipt links, and a free-tier messaging note. Router updated to lazy-load billing page.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete billing system</name>
  <action>
Human verification of the complete billing system built across all 3 plans.

What was built:
- Billing page at /billing with 3 credit pack cards (50k, 200k, 1M impressions)
- Credit balance badge in app header (always visible, polls every 30 seconds)
- Transaction history table with receipt links
- Low-balance warning toast system
- Stripe Checkout integration (create-checkout + stripe-webhook Edge Functions)
- Daily metrics rollup function with pg_cron schedule

How to verify:
1. Start dev server: `pnpm run dev` from apps/web
2. Log in and verify credit balance badge appears in the header (shows "0" initially)
3. Click the balance badge -- should navigate to /billing
4. On /billing page, verify:
   a. Balance summary card shows current credit balance
   b. Three credit pack cards (Starter 50k, Growth 200k, Scale 1M) are displayed in a grid
   c. "Creating and previewing ads is always free" note is visible at bottom
   d. Transaction history section shows empty state message
5. (If Stripe is configured) Click "Buy Now" on a pack -- should redirect to Stripe Checkout
6. Navigate to /creatives and verify you can still create and preview ads with 0 credits (BILL-05)
7. Check that no credit-related errors appear in console during normal navigation
  </action>
  <verify>User confirms all verification steps pass</verify>
  <done>User types "approved" or describes issues to fix</done>
</task>

</tasks>

<verification>
- /billing route renders billing page (not placeholder)
- Three credit pack cards displayed with Buy Now buttons
- Credit balance visible in header for all authenticated pages
- Transaction table shows purchase history (empty state if no purchases)
- Success banner appears when returning from Stripe with ?success=true
- Editor and preview work without any credit balance (BILL-05 verification)
- Low-balance warning toast fires when balance is below 10% of last purchase
</verification>

<success_criteria>
- User can navigate to /billing and see credit pack options
- Clicking Buy Now invokes create-checkout Edge Function and redirects to Stripe
- After Stripe payment, user returns to /billing?success=true and sees confirmation
- Transaction history table displays with date, type, credits, balance, receipt link
- Creating/previewing ads works regardless of credit balance (free to create, pay to serve)
- Credit balance in header reflects current balance and updates automatically
</success_criteria>

<output>
After completion, create `.planning/phases/09-billing-credit-system/09-03-SUMMARY.md`
</output>
