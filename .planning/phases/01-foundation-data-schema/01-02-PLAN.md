---
phase: 01-foundation-data-schema
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - pnpm-workspace.yaml
  - package.json
  - apps/web/package.json
  - apps/web/index.html
  - apps/web/vite.config.ts
  - apps/web/tsconfig.json
  - apps/web/tsconfig.app.json
  - apps/web/tsconfig.node.json
  - apps/web/src/main.tsx
  - apps/web/src/App.tsx
  - apps/web/src/index.css
  - apps/web/src/vite-env.d.ts
  - apps/web/src/lib/supabase.ts
  - apps/web/components.json
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/src/index.ts
  - packages/shared/src/database.types.ts
  - packages/shared/src/constants.ts
  - packages/ad-sdk/package.json
  - packages/ad-sdk/tsconfig.json
  - packages/ad-sdk/src/index.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-05
user_setup: []

must_haves:
  truths:
    - "React + TypeScript frontend project builds and runs locally without errors"
    - "Supabase client is configured and can connect to the remote project"
    - "pnpm monorepo workspace resolves cross-package imports"
    - "Tailwind CSS 4 utility classes render correctly in the browser"
    - "Auto-generated database types are importable from the shared package"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Monorepo workspace configuration"
      contains: "apps/*"
    - path: "apps/web/vite.config.ts"
      provides: "Vite 7 build configuration with Tailwind CSS 4 plugin and path aliases"
      contains: "@tailwindcss/vite"
    - path: "apps/web/src/lib/supabase.ts"
      provides: "Supabase client singleton with Database type generic"
      contains: "createClient<Database>"
    - path: "apps/web/src/App.tsx"
      provides: "Root component with QueryClientProvider"
      contains: "QueryClientProvider"
    - path: "packages/shared/src/database.types.ts"
      provides: "Auto-generated TypeScript types from Supabase schema"
      contains: "advertisers"
    - path: "packages/shared/src/constants.ts"
      provides: "Shared constants including event types"
      contains: "EVENT_TYPES"
    - path: "apps/web/src/index.css"
      provides: "Tailwind CSS 4 import"
      contains: "@import"
  key_links:
    - from: "apps/web/src/lib/supabase.ts"
      to: "packages/shared/src/database.types.ts"
      via: "import type { Database }"
      pattern: "import.*Database.*@scrolltoday/shared"
    - from: "apps/web/src/lib/supabase.ts"
      to: ".env.development"
      via: "import.meta.env.VITE_SUPABASE_URL"
      pattern: "import\\.meta\\.env\\.VITE_SUPABASE"
    - from: "apps/web/src/App.tsx"
      to: "@tanstack/react-query"
      via: "QueryClientProvider wrapper"
      pattern: "QueryClientProvider"
    - from: "apps/web/package.json"
      to: "packages/shared/package.json"
      via: "workspace:* dependency"
      pattern: "@scrolltoday/shared.*workspace"
---

<objective>
Scaffold the pnpm monorepo with React + TypeScript frontend (Vite 7, Tailwind CSS 4, shadcn/ui), shared types package, and ad-sdk placeholder. Wire the Supabase client with auto-generated types and verify the dev server runs.

Purpose: The frontend project structure is the shell that all UI features build into. Getting the monorepo, build tooling, Supabase client, and state management providers right from the start prevents costly refactoring later.
Output: A running `pnpm dev` server showing a basic page, with Supabase client connected, shared types importable, and Tailwind CSS 4 styling working.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-data-schema/01-CONTEXT.md
@.planning/phases/01-foundation-data-schema/01-RESEARCH.md
@.planning/phases/01-foundation-data-schema/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold pnpm monorepo with web app, shared package, and ad-sdk placeholder</name>
  <files>
    pnpm-workspace.yaml
    package.json
    apps/web/package.json
    apps/web/index.html
    apps/web/vite.config.ts
    apps/web/tsconfig.json
    apps/web/tsconfig.app.json
    apps/web/tsconfig.node.json
    apps/web/src/main.tsx
    apps/web/src/App.tsx
    apps/web/src/index.css
    apps/web/src/vite-env.d.ts
    apps/web/components.json
    packages/shared/package.json
    packages/shared/tsconfig.json
    packages/shared/src/index.ts
    packages/shared/src/constants.ts
    packages/ad-sdk/package.json
    packages/ad-sdk/tsconfig.json
    packages/ad-sdk/src/index.ts
  </files>
  <action>
    **Step 1: Create monorepo root**

    Create `pnpm-workspace.yaml`:
    ```yaml
    packages:
      - 'apps/*'
      - 'packages/*'
    ```

    Create root `package.json`:
    ```json
    {
      "name": "scrolltoday",
      "private": true,
      "scripts": {
        "dev": "pnpm --filter @scrolltoday/web dev",
        "build": "pnpm --filter @scrolltoday/web build",
        "preview": "pnpm --filter @scrolltoday/web preview",
        "typecheck": "pnpm -r typecheck",
        "db:push": "npx supabase db push",
        "db:types": "npx supabase gen types typescript --project-id $SUPABASE_PROJECT_REF --schema public > packages/shared/src/database.types.ts"
      },
      "engines": {
        "node": ">=20"
      }
    }
    ```

    **Step 2: Scaffold web app using Vite**

    Run `pnpm create vite@latest apps/web -- --template react-ts` to create the React + TypeScript app.
    If the interactive prompt blocks, manually create the file structure instead:

    Create `apps/web/package.json` with:
    - name: "@scrolltoday/web"
    - dependencies: react (^19), react-dom (^19), @supabase/supabase-js (^2), @tanstack/react-query (^5), @scrolltoday/shared (workspace:*)
    - devDependencies: @types/react, @types/react-dom, @types/node, @vitejs/plugin-react, typescript (^5.7), vite (^7), tailwindcss (^4.1), @tailwindcss/vite

    Create `apps/web/index.html` with standard Vite React template pointing to `/src/main.tsx`.

    Create `apps/web/vite.config.ts`:
    - Import and register plugins: react(), tailwindcss()
    - Set path alias: '@' -> './src'
    - Set envDir to '../../' (reads .env from monorepo root)

    Create `apps/web/tsconfig.json` as project references file pointing to tsconfig.app.json and tsconfig.node.json.

    Create `apps/web/tsconfig.app.json`:
    - target: ES2020, module: ESNext, moduleResolution: bundler
    - jsx: react-jsx
    - paths: { "@/*": ["./src/*"] }
    - include: ["src"]
    - strict: true

    Create `apps/web/tsconfig.node.json`:
    - For vite.config.ts: target ES2022, module ESNext, moduleResolution bundler
    - include: ["vite.config.ts"]

    Create `apps/web/src/vite-env.d.ts` with:
    ```typescript
    /// <reference types="vite/client" />

    interface ImportMetaEnv {
      readonly VITE_SUPABASE_URL: string
      readonly VITE_SUPABASE_ANON_KEY: string
    }

    interface ImportMeta {
      readonly env: ImportMetaEnv
    }
    ```

    Create `apps/web/src/index.css`:
    ```css
    @import "tailwindcss";
    ```

    Create `apps/web/src/main.tsx`:
    ```tsx
    import { StrictMode } from 'react'
    import { createRoot } from 'react-dom/client'
    import App from './App'
    import './index.css'

    createRoot(document.getElementById('root')!).render(
      <StrictMode>
        <App />
      </StrictMode>
    )
    ```

    Create `apps/web/src/App.tsx`:
    ```tsx
    import { QueryClient, QueryClientProvider } from '@tanstack/react-query'

    const queryClient = new QueryClient({
      defaultOptions: {
        queries: {
          staleTime: 1000 * 60 * 5,
          retry: 1,
        },
      },
    })

    function App() {
      return (
        <QueryClientProvider client={queryClient}>
          <div className="min-h-screen bg-gray-50 flex items-center justify-center">
            <div className="text-center">
              <h1 className="text-3xl font-bold text-gray-900">ScrollToday</h1>
              <p className="mt-2 text-gray-600">Foundation Ready</p>
            </div>
          </div>
        </QueryClientProvider>
      )
    }

    export default App
    ```

    **Step 3: Initialize shadcn/ui**

    Run `pnpm dlx shadcn@latest init` inside apps/web/ with these options:
    - Style: New York
    - Base color: Neutral
    - CSS variables: Yes

    If the interactive prompt blocks, manually create `apps/web/components.json` with the shadcn/ui configuration:
    ```json
    {
      "$schema": "https://ui.shadcn.com/schema.json",
      "style": "new-york",
      "rsc": false,
      "tsx": true,
      "tailwind": {
        "config": "",
        "css": "src/index.css",
        "baseColor": "neutral",
        "cssVariables": true
      },
      "aliases": {
        "components": "@/components",
        "utils": "@/lib/utils",
        "ui": "@/components/ui",
        "hooks": "@/hooks"
      }
    }
    ```

    Create `apps/web/src/lib/utils.ts` with the shadcn cn() utility:
    ```typescript
    import { type ClassValue, clsx } from "clsx"
    import { twMerge } from "tailwind-merge"

    export function cn(...inputs: ClassValue[]) {
      return twMerge(clsx(inputs))
    }
    ```
    Add clsx and tailwind-merge to apps/web dependencies.

    **Step 4: Create shared package**

    Create `packages/shared/package.json`:
    ```json
    {
      "name": "@scrolltoday/shared",
      "version": "0.0.1",
      "private": true,
      "main": "src/index.ts",
      "types": "src/index.ts",
      "scripts": {
        "typecheck": "tsc --noEmit"
      }
    }
    ```

    Create `packages/shared/tsconfig.json`:
    - target: ES2020, module: ESNext, moduleResolution: bundler
    - strict: true, declaration: true
    - include: ["src"]

    Move the `database.types.ts` file generated in Plan 01 to `packages/shared/src/database.types.ts`.
    If the file does not exist yet (Plan 01 placed it at root), regenerate it:
    `npx supabase gen types typescript --project-id <PROJECT_REF> --schema public > packages/shared/src/database.types.ts`

    Create `packages/shared/src/constants.ts`:
    ```typescript
    export const EVENT_TYPES = [
      'impression_served',
      'impression_viewable',
      'engagement',
      'click',
      'video_play',
      'video_pause',
      'video_complete',
      'expand',
      'collapse',
      'close',
    ] as const

    export type EventType = typeof EVENT_TYPES[number]

    export const CREATIVE_STATUSES = ['draft', 'active', 'paused', 'archived'] as const
    export type CreativeStatus = typeof CREATIVE_STATUSES[number]

    export const CAMPAIGN_STATUSES = ['draft', 'active', 'paused', 'completed'] as const
    export type CampaignStatus = typeof CAMPAIGN_STATUSES[number]

    export const USER_ROLES = ['super_admin', 'advertiser'] as const
    export type UserRole = typeof USER_ROLES[number]

    export const STORAGE_BUCKET = 'creative-assets'
    export const MAX_FILE_SIZE = 52428800 // 50MB

    export const ALLOWED_MIME_TYPES = [
      'image/png',
      'image/jpeg',
      'image/gif',
      'image/webp',
      'image/svg+xml',
      'video/mp4',
      'video/webm',
      'application/javascript',
      'text/html',
      'text/css',
    ] as const
    ```

    Create `packages/shared/src/index.ts`:
    ```typescript
    export type { Database } from './database.types'
    export * from './constants'
    ```

    **Step 5: Create ad-sdk placeholder**

    Create `packages/ad-sdk/package.json`:
    ```json
    {
      "name": "@scrolltoday/ad-sdk",
      "version": "0.0.1",
      "private": true,
      "main": "src/index.ts",
      "types": "src/index.ts",
      "dependencies": {
        "@scrolltoday/shared": "workspace:*"
      },
      "scripts": {
        "typecheck": "tsc --noEmit"
      }
    }
    ```

    Create `packages/ad-sdk/tsconfig.json` (same settings as shared package).

    Create `packages/ad-sdk/src/index.ts`:
    ```typescript
    // Ad SDK - Phase 8 implementation
    // Placeholder for ad serving client library
    export const AD_SDK_VERSION = '0.0.1'
    ```

    **Step 6: Install all dependencies**

    From project root, run: `pnpm install`
    This installs all workspace packages and resolves workspace:* references.
  </action>
  <verify>
    1. `pnpm install` completes without errors
    2. `pnpm --filter @scrolltoday/web exec tsc --noEmit` passes (TypeScript compiles)
    3. `pnpm dev` starts Vite dev server without errors (shows local URL)
    4. `packages/shared/src/database.types.ts` exists and contains table definitions
    5. `pnpm-workspace.yaml` exists with correct workspace paths
  </verify>
  <done>
    pnpm monorepo is scaffolded with 3 packages (web, shared, ad-sdk).
    Web app builds with Vite 7, React 19, TypeScript, Tailwind CSS 4, and shadcn/ui config.
    Shared package exports auto-generated Database types and shared constants.
    All workspace cross-references resolve correctly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire Supabase client, verify full-stack connection, and confirm dev server</name>
  <files>
    apps/web/src/lib/supabase.ts
    apps/web/src/App.tsx
  </files>
  <action>
    **Step 1: Create Supabase client singleton**

    Create `apps/web/src/lib/supabase.ts`:
    ```typescript
    import { createClient } from '@supabase/supabase-js'
    import type { Database } from '@scrolltoday/shared'

    const supabaseUrl = import.meta.env.VITE_SUPABASE_URL
    const supabaseAnonKey = import.meta.env.VITE_SUPABASE_ANON_KEY

    if (!supabaseUrl || !supabaseAnonKey) {
      throw new Error(
        'Missing Supabase environment variables. Copy .env.example to .env.development and fill in your Supabase credentials.'
      )
    }

    export const supabase = createClient<Database>(supabaseUrl, supabaseAnonKey, {
      auth: {
        autoRefreshToken: true,
        persistSession: true,
        detectSessionInUrl: true,
      },
    })
    ```

    **Step 2: Update App.tsx to verify Supabase connection**

    Update `apps/web/src/App.tsx` to import the supabase client and display connection status.
    Add a simple useEffect that calls `supabase.from('advertisers').select('count')` to verify connectivity.
    Display "Connected to Supabase" or an error message. This is temporary verification UI -- it will be replaced in Phase 3.

    The component should:
    - Import supabase from '@/lib/supabase'
    - Use React useState and useEffect to test the connection on mount
    - Show a status indicator (green = connected, red = error)
    - Keep the QueryClientProvider wrapper
    - Use Tailwind CSS classes for styling to confirm Tailwind is working

    **Step 3: Verify the full stack**

    1. Run `pnpm dev` and open the URL in a browser
    2. Verify the page renders with Tailwind CSS styles (centered text, gray background)
    3. Verify the Supabase connection indicator shows "Connected"
    4. Check browser console for no errors
    5. Run `pnpm build` to verify production build works

    **Step 4: Verify directory structure**

    The final project structure should match:
    ```
    scrolltoday/
    ├── pnpm-workspace.yaml
    ├── package.json
    ├── .env.development (gitignored)
    ├── .env.production (gitignored)
    ├── .env.example
    ├── .gitignore
    ├── supabase/
    │   ├── config.toml
    │   ├── migrations/
    │   │   ├── 20260219000000_initial_schema.sql
    │   │   └── 20260219000001_storage_bucket.sql
    │   └── seed.sql
    ├── apps/
    │   └── web/
    │       ├── package.json
    │       ├── index.html
    │       ├── vite.config.ts
    │       ├── tsconfig.json
    │       ├── tsconfig.app.json
    │       ├── tsconfig.node.json
    │       ├── components.json
    │       └── src/
    │           ├── main.tsx
    │           ├── App.tsx
    │           ├── index.css
    │           ├── vite-env.d.ts
    │           └── lib/
    │               ├── supabase.ts
    │               └── utils.ts
    ├── packages/
    │   ├── shared/
    │   │   ├── package.json
    │   │   ├── tsconfig.json
    │   │   └── src/
    │   │       ├── index.ts
    │   │       ├── database.types.ts
    │   │       └── constants.ts
    │   └── ad-sdk/
    │       ├── package.json
    │       ├── tsconfig.json
    │       └── src/
    │           └── index.ts
    └── pnpm-lock.yaml
    ```
  </action>
  <verify>
    1. `pnpm dev` starts successfully and shows local URL (http://localhost:5173 or similar)
    2. Opening the URL in browser shows "ScrollToday" heading with Tailwind CSS styling (centered, gray background, proper typography)
    3. Supabase connection status shows "Connected" (or green indicator)
    4. Browser console has no errors
    5. `pnpm build` completes successfully and produces `apps/web/dist/` folder
    6. Import `import type { Database } from '@scrolltoday/shared'` resolves in `apps/web/src/lib/supabase.ts`
    7. Import `import { EVENT_TYPES } from '@scrolltoday/shared'` resolves
  </verify>
  <done>
    Supabase client singleton is created with auto-generated Database type generic.
    Client connects to the remote Supabase project and can query tables.
    Dev server runs locally with hot reload.
    Production build succeeds.
    Cross-package imports (@scrolltoday/shared -> @scrolltoday/web) resolve correctly.
    Tailwind CSS 4 classes render properly in the browser.
  </done>
</task>

</tasks>

<verification>
1. Full project structure exists with monorepo layout (apps/web, packages/shared, packages/ad-sdk)
2. `pnpm install` resolves all dependencies including workspace references
3. `pnpm dev` starts Vite dev server serving the React app
4. `pnpm build` produces production build without errors
5. Supabase client connects to remote project and can query tables
6. Database types are auto-generated and importable across packages
7. Tailwind CSS 4 utility classes apply correctly in the browser
8. shadcn/ui is configured (components.json exists, cn() utility works)
</verification>

<success_criteria>
- React + TypeScript frontend builds and runs locally at localhost
- Supabase client is configured with Database type generic and connects to remote project
- pnpm monorepo with 3 packages resolves all workspace dependencies
- Tailwind CSS 4 styles render correctly (visible in browser)
- Auto-generated database types are importable from @scrolltoday/shared
- Production build (`pnpm build`) succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-schema/01-02-SUMMARY.md`
</output>
